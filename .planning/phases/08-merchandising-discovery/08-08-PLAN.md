---
phase: 08-merchandising-discovery
plan: 08
type: execute
wave: 4
depends_on: [08-03, 08-05, 08-07]
files_modified:
  - app/collections/page.tsx
  - app/collections/[slug]/page.tsx
  - components/Nav.tsx
autonomous: true
requirements: [DISC-04, CATL-01]
user_setup: []

must_haves:
  truths:
    - "A /collections index lists the seeded curated collections, each linking to its page (DISC-04)"
    - "A /collections/[slug] page lists only that collection's products using the same paginated grid as /shop (D-12, CATL-01)"
    - "Collections are reachable from the nav (D-14 discoverability of the curated grouping)"
    - "An unknown collection slug renders a friendly not-found, not a crash"
  artifacts:
    - "app/collections/page.tsx (collection index)"
    - "app/collections/[slug]/page.tsx (collection detail, reuses ProductGrid + Pagination)"
    - "components/Nav.tsx (Collections link)"
  key_links:
    - "collection detail calls getCatalog({ collection: slug, ...searchParams }) -> ProductGrid + Pagination"
    - "getCollectionBySlug guards unknown slugs -> notFound()"
    - "Nav links to /collections"
---

<objective>
Deliver curated collections as their own browsable, scalable routes (DISC-04, D-10,
D-12): a `/collections` index listing the seeded collections and a
`/collections/[slug]` detail page that reuses the exact paginated grid + pagination
from `/shop`, filtered to the collection via `getCatalog`'s collection parameter.
Collections are a curated grouping separate from category, and — per D-11 — management
(create/edit) is explicitly NOT built here; it is deferred to the Phase 9 admin. Add a
Collections entry to the nav so the curated grouping is discoverable.

Purpose: Let customers browse curated collections that scale beyond a handful of items,
built on the same query and grid infrastructure as the main shop.
Output: the two collection routes and a Collections nav link.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-merchandising-discovery/08-CONTEXT.md
@.planning/phases/08-merchandising-discovery/08-RESEARCH.md
@app/shop/page.tsx
@components/ProductGrid.tsx
@components/shop/Pagination.tsx
@components/Nav.tsx
@lib/products.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collections index page</name>
  <read_first>lib/products.ts getCollections (from 08-03), app/shop/page.tsx (header + container styling), 08-CONTEXT.md D-10/D-11/D-12</read_first>
  <files>app/collections/page.tsx</files>
  <action>
    Create `app/collections/page.tsx` as an async Server Component (`dynamic =
    "force-dynamic"`) that calls `getCollections()` and renders a header consistent with
    `/shop` plus a grid of collection cards — each showing the collection name and
    description and linking to `/collections/[slug]`. Style with the locked aesthetic
    (Fraunces headings, eyebrow labels, ink/sepia/cream). Show a friendly empty state if
    no collections exist. Do NOT add any create/edit affordance — collection management
    is deferred to Phase 9 (D-11); this is read-only browsing.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    `npx tsc --noEmit` passes; `/collections` lists the seeded collections, each linking
    to its detail page; no management UI is present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Collection detail page (reuses the paginated grid)</name>
  <read_first>app/shop/page.tsx (searchParams -> getCatalog + ProductGrid + Suspense-wrapped Pagination pattern), lib/products.ts getCatalog + getCollectionBySlug, components/ProductGrid.tsx, components/shop/Pagination.tsx</read_first>
  <files>app/collections/[slug]/page.tsx</files>
  <action>
    Create `app/collections/[slug]/page.tsx` as an async Server Component (`dynamic =
    "force-dynamic"`) taking `params.slug` and `searchParams` (at least `page`, and
    optionally sort). Call `getCollectionBySlug(params.slug)`; if null, call `notFound()`
    (friendly branded 404). Render the collection name/description header, then call
    `getCatalog({ collection: params.slug, sort: searchParams.sort, page:
    searchParams.page })` and render the shared `ProductGrid` with the returned products
    and a `Pagination` (passed `totalPages`) wrapped in `<Suspense>` exactly like `/shop`
    (Pitfall 1). This reuses the same scalable query + grid path (D-12, CATL-01) — do not
    write a separate collection-specific query. A minimal sort control may be included but
    the full FilterPanel is optional here; pagination is required so collections scale
    beyond a handful of items.
  </action>
  <verify>
    <automated>npx next build</automated>
  </verify>
  <done>
    `npx next build` succeeds; `/collections/[slug]` lists only that collection's products
    via getCatalog, paginates like `/shop`, and returns a friendly not-found for an
    unknown slug.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add a Collections link to the nav</name>
  <read_first>components/Nav.tsx (LINKS array + mobile menu; note the Wishlist entry added by 08-07 must be preserved)</read_first>
  <files>components/Nav.tsx</files>
  <action>
    Update `components/Nav.tsx` to add a "Collections" entry linking to `/collections` in
    the left-side `LINKS` list (and therefore the mobile menu, which maps the same
    array), placed sensibly among Shop/Outerwear/Knitwear/Accessories. Preserve the
    Wishlist affordance and cart control added earlier — only add the Collections link,
    do not remove or restructure existing entries.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx next build</automated>
  </verify>
  <done>
    `npx tsc --noEmit` and `npx next build` pass; the nav shows a Collections link (in
    both desktop and mobile menus) pointing to `/collections`, with the Wishlist and Cart
    controls intact.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (URL slug + page) -> collection Server Components -> getCatalog | slug and page are untrusted; validated before use |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | collection slug + page params in the query | high | mitigate | slug and page flow through `getCollectionBySlug` (parameterized `where: { slug }`) and the validated `getCatalog` builder — parameterized Prisma only, no raw SQL. An unknown slug yields `notFound()`, not an error path. |
| T-08-02 | Denial of Service | `page` param on a collection route | medium | mitigate | Same `parsePage` clamp as /shop (proven in 08-03) bounds skip/take; the grid never loads the full collection. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes for both routes.
- `npx next build` succeeds (Suspense boundary on collection pagination — Pitfall 1).
- Browser (human-check, at phase verify): open `/collections`, click a collection, page
  through its products; hit an unknown `/collections/does-not-exist` and confirm the
  branded not-found; confirm the nav Collections link works on desktop and mobile.
</verification>

<success_criteria>
- Curated collections are browsable via their own routes, separate from category (DISC-04, D-10).
- Collection pages reuse the scalable paginated grid (D-12, CATL-01).
- Collections are discoverable from the nav; no management UI is built (D-11).
</success_criteria>

<output>
Create `.planning/phases/08-merchandising-discovery/08-08-SUMMARY.md` when done.
</output>
