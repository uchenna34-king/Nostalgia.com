---
phase: 08-merchandising-discovery
plan: 06
type: execute
wave: 3
depends_on: [08-02, 08-03, 08-04]
files_modified:
  - components/Gallery.tsx
  - app/product/[slug]/page.tsx
autonomous: true
requirements: [PDP-01]
user_setup: []

must_haves:
  truths:
    - "The PDP shows a large main image with selectable thumbnails driven by the product's ordered ProductImage set; clicking a thumbnail swaps the main image (D-08, PDP-01)"
    - "The PDP surfaces richer detail content (materials/care) where the product has it (D-08)"
    - "A WishlistButton on the PDP lets the customer save the product (D-14)"
    - "Related products are fetched via the scalable getCatalog path, not an unbounded query (CATL-01 consistency)"
  artifacts:
    - "components/Gallery.tsx (client: main image + thumbnail selection)"
    - "app/product/[slug]/page.tsx (richer PDP wiring)"
  key_links:
    - "Gallery receives images: string[] (ordered) + product name for alt text"
    - "PDP passes product.images to Gallery, renders materials/care, mounts WishlistButton"
---

<objective>
Enrich the product detail page into the multi-image, richer-content experience the
phase promises (D-08, PDP-01), driven by the per-product `ProductImage` set built in
08-02. Replace today's static image grid with a `Gallery` client component — a large
main image plus selectable thumbnails that swap the main image on click — and surface
the richer `materials`/`care` copy where present. Add a `WishlistButton` so customers
can save from the PDP (D-14), and route the "related" products through the scalable
`getCatalog` path so no PDP does an unbounded fetch (CATL-01 consistency).

Purpose: Deliver the customer-facing richer PDP that the new image model exists to power.
Output: `components/Gallery.tsx` and a rewritten `app/product/[slug]/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-merchandising-discovery/08-CONTEXT.md
@.planning/phases/08-merchandising-discovery/08-RESEARCH.md
@app/product/[slug]/page.tsx
@components/ProductCard.tsx
@components/AddToCart.tsx
@lib/products.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gallery client component (main image + thumbnails)</name>
  <read_first>app/product/[slug]/page.tsx (current image grid markup + aspect ratios), 08-RESEARCH.md Recommended Project Structure (Gallery.tsx) + Anti-Patterns (hand-roll, no carousel lib)</read_first>
  <files>components/Gallery.tsx</files>
  <action>
    Create `components/Gallery.tsx` (`"use client"`) taking `images: string[]` (ordered
    URLs) and `name: string`. Hold a `selectedIndex` in `useState` (default 0); render a
    large main image (`aspect-[3/4]`, `object-cover`, `bg-cream-dark`) showing
    `images[selectedIndex]`, and below/beside it a row of thumbnail buttons for each
    image. Clicking a thumbnail sets `selectedIndex`; the active thumbnail is visually
    marked (ink border) and carries `aria-current`/`aria-pressed` for accessibility;
    thumbnails are keyboard-focusable buttons with meaningful `aria-label`s (e.g. "View
    image N of M"). Alt text on the main image uses the product `name`. If there is only
    one image, render just the main image with no thumbnail row. Do NOT pull in a
    carousel library (RESEARCH Anti-Patterns) — hand-roll to fit the bespoke aesthetic;
    keep transitions subtle and honor reduced-motion. Use `img` with the existing
    eslint-disable pattern already used in the PDP/ProductCard.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    `npx tsc --noEmit` passes; Gallery renders an ordered thumbnail set, swaps the main
    image on thumbnail click, marks the active thumbnail accessibly, and degrades to a
    single image when only one exists.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the PDP to use Gallery, richer details, and WishlistButton</name>
  <read_first>app/product/[slug]/page.tsx (current layout, related products), components/Gallery.tsx (this plan), components/WishlistButton.tsx (from 08-04), lib/products.ts getCatalog + getProductBySlug, 08-CONTEXT.md D-08/D-14</read_first>
  <files>app/product/[slug]/page.tsx</files>
  <action>
    Rewrite `app/product/[slug]/page.tsx` (keep `dynamic = "force-dynamic"` and the
    `notFound()` guard). Replace the inline image grid with `<Gallery images={
    product.images} name={product.name} />`. In the details column, keep the eyebrow /
    title / price / description / `AddToCart`, and add a richer-details section that
    renders `product.materials` and `product.care` when non-null (e.g. a small "Materials
    & Care" block styled with the existing uppercase-tracking list treatment); when both
    are null, omit the section cleanly. Mount a `WishlistButton` for this product near the
    add-to-cart action (D-14 — save from the PDP), passing the minimal
    `{slug,name,price,image:product.images[0]}`. Replace the related-products fetch:
    instead of `getProducts(product.category)` (unbounded), call
    `getCatalog({ category: product.category, page: 1 })`, exclude the current slug, and
    take the first few for the "More from {category}" grid (CATL-01 — bounded, indexed).
    Preserve the breadcrumb and overall layout/aesthetic.
  </action>
  <verify>
    <automated>npx next build</automated>
  </verify>
  <done>
    `npx next build` succeeds; the PDP renders the multi-image Gallery, shows
    materials/care when present, includes a working WishlistButton, and sources related
    products via getCatalog.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server (DB product data) -> PDP render | Product image URLs/copy are trusted seed/DB data |
| client -> Gallery state | Thumbnail selection is client-only, no server effect |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-06 | Information Disclosure | Image URLs / richer copy rendered on the PDP | low | accept | All image paths and materials/care copy come from the DB/seed (trusted), rendered as static `img src` and text — no user input is reflected. Alt text uses the product name (server data). |
| T-08-01 | Tampering | related-products category param via getCatalog | low | mitigate | The category comes from the loaded product (server data) and still flows through the validated getCatalog builder. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes for Gallery.
- `npx next build` succeeds.
- Browser (human-check, at phase verify): open a product, click each thumbnail and
  confirm the main image swaps; confirm materials/care show for a product that has them;
  save to wishlist from the PDP and confirm it reflects in the nav/wishlist page.
</verification>

<success_criteria>
- PDP shows a multi-image gallery driven by ordered ProductImage data (PDP-01, D-08).
- Richer materials/care content appears where data allows (D-08).
- Customers can save to wishlist from the PDP (D-14).
- Related products use the scalable query path (CATL-01).
</success_criteria>

<output>
Create `.planning/phases/08-merchandising-discovery/08-06-SUMMARY.md` when done.
</output>
