---
phase: 08-merchandising-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vitest.config.ts
  - tests/smoke.test.ts
autonomous: true
requirements: [DISC-01, DISC-02, DISC-03, CATL-01, WISH-01]
user_setup: []

must_haves:
  truths:
    - "`npx vitest run` executes and reports a passing suite"
    - "`use-debounce` is installed and importable for the Wave 3 debounced search input"
    - "TypeScript path alias `@/` resolves inside test files (matches app tsconfig)"
  artifacts:
    - "vitest.config.ts"
    - "tests/smoke.test.ts"
    - "package.json with vitest + use-debounce dependencies"
  key_links:
    - "vitest.config.ts alias `@` -> project root, mirroring tsconfig paths"
    - "package.json test tooling has no runtime effect on `next build`"
---

<objective>
Establish the automated test harness this phase's pure-logic units run on, and add
the one runtime dependency (`use-debounce`) the Wave 3 search box needs. Per the
Phase 8 VALIDATION strategy, all pure logic (catalog query builder, price parsing,
pagination math, wishlist reducer) is extracted into testable modules and covered by
Vitest; UI flows are verified in-browser. This plan is the "Wave 0" test setup: it
installs Vitest + a jsdom-capable config and proves the runner works before any test
is authored in later waves.

Purpose: Make DISC-01/02/03, CATL-01, and WISH-01 pure logic unit-testable so the
query builder and wishlist reducer are proven before the UI is wired.
Output: `vitest.config.ts`, a green `tests/smoke.test.ts`, and `use-debounce` +
Vitest dev tooling in `package.json`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-merchandising-discovery/08-RESEARCH.md
@.planning/phases/08-merchandising-discovery/08-VALIDATION.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install test tooling and use-debounce</name>
  <read_first>package.json (scripts, dependencies, devDependencies)</read_first>
  <files>package.json, package-lock.json</files>
  <action>
    Install the runtime dependency and the dev test tooling in one step, pinning to
    the researched versions. Runtime: `use-debounce@^10.1.1` (RESEARCH Package
    Legitimacy Audit verdict OK — the exact library Next.js's own search/pagination
    tutorial uses; do NOT hand-roll a debounce). Dev: `vitest`, `jsdom`,
    `@testing-library/react`, and `@vitejs/plugin-react` (standard Vite-native test
    stack; supports both the pure-TS logic tests and the optional jsdom component
    tests in later waves). Run `npm install use-debounce` then
    `npm install -D vitest jsdom @testing-library/react @vitejs/plugin-react`. Add a
    `"test": "vitest run"` script to package.json scripts (CI-style, no watch mode).
    Do NOT upgrade `next` or `prisma` — 14.2.35 / 5.22.0 are locked and already
    installed (RESEARCH: registry has newer majors, out of scope). The existing
    `postinstall: prisma generate` must remain.
  </action>
  <verify>
    <automated>node -e "require.resolve('use-debounce'); require.resolve('vitest'); console.log('deps-ok')"</automated>
  </verify>
  <done>
    `use-debounce`, `vitest`, `jsdom`, `@testing-library/react`, and
    `@vitejs/plugin-react` resolve from node_modules; package.json has a
    `test: vitest run` script; `next`/`prisma` versions unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add vitest.config.ts and a smoke test</name>
  <read_first>package.json (path usage), any existing tsconfig.json for the `@/*` alias mapping</read_first>
  <files>vitest.config.ts, tests/smoke.test.ts</files>
  <action>
    Create `vitest.config.ts` at the project root using `@vitejs/plugin-react` and
    Vitest's `defineConfig`. Set `test.environment` to `jsdom` (works for pure-TS
    tests too and enables later Gallery/Wishlist component tests), `test.globals` to
    true, and `test.include` to cover `tests/**/*.test.{ts,tsx}`. Register a resolve
    alias mapping `@` to the project root directory (use `fileURLToPath(new URL('.',
    import.meta.url))`) so test imports like `@/lib/catalog` resolve exactly as they
    do in the Next.js app. Create `tests/smoke.test.ts` with a single trivial test
    asserting basic arithmetic (proves the runner is wired) — this file is replaced
    by real suites in Waves 2-3 but confirms the harness runs now. Do NOT add any
    watch flags. Keep the config free of Prisma/DB setup — this phase's logic tests
    are pure functions per the VALIDATION strategy and need no test database.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>
    `npx vitest run` discovers and passes `tests/smoke.test.ts` and exits 0; the `@`
    alias resolves in test files; no watch mode is triggered.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> project | Third-party packages installed into the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-SC | Tampering | npm installs (use-debounce + vitest/jsdom/testing-library/plugin-react) | medium | mitigate | Install only researched, pinned versions. `use-debounce` audited OK in RESEARCH (Package Legitimacy Audit: ~8yr old, ~6.47M weekly downloads, no postinstall script). Vitest/jsdom/@testing-library/@vitejs are canonical, ubiquitous dev-only test tooling with no runtime footprint in `next build`. No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages introduced, so no blocking legitimacy checkpoint is required. |
</threat_model>

<verification>
- `npx vitest run` passes (harness proven).
- `node -e "require.resolve('use-debounce')"` succeeds.
- `npx next build` still succeeds (test tooling has no runtime effect) — run at wave merge.
</verification>

<success_criteria>
- Vitest runs a green suite via `npx vitest run`.
- `use-debounce` is installed and importable.
- The `@/` alias resolves in tests, matching the app.
- No production dependency or framework version was changed beyond adding `use-debounce`.
</success_criteria>

<output>
Create `.planning/phases/08-merchandising-discovery/08-01-SUMMARY.md` when done.
</output>
