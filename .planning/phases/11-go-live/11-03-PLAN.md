---
phase: 11-go-live
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - components/account/OrderList.tsx
  - components/account/AccountAuthActions.tsx
  - app/account/page.tsx
  - tests/account-orders.test.tsx
autonomous: true
requirements: [LIVE-04]

must_haves:
  truths:
    - "A signed-in customer visiting /account sees a list of their own orders (reference, date, items, total, status), newest first."
    - "A signed-in customer with zero orders sees a branded empty state with a heading, body copy, and a 'Browse the collection' CTA — never a blank region or a broken-looking page."
    - "A signed-out visitor to /account sees the existing signed-out branch with a working Sign in button; a signed-in customer keeps a working Sign out button."
    - "No customer can ever see another customer's order in the list — the query is scoped to session.user.id and never runs unscoped."
    - "Every order status is conveyed by a visible text label, not by colour alone (WCAG 1.4.1), and the paid pill clears 4.5:1 on cream-dark."
    - "On viewports under 640px the order list renders as stacked cards; the page never scrolls horizontally."
  artifacts:
    - components/account/OrderList.tsx
    - components/account/AccountAuthActions.tsx
    - app/account/page.tsx
    - tests/account-orders.test.tsx
  key_links:
    - "app/account/page.tsx -> getServerSession(authOptions) -> session.user.id -> prisma.order.findMany({ where: { userId } })"
    - "prisma Order rows -> toOrderRows() -> <OrderList orders={rows} /> -> table (>=640px) / card list (<640px)"
    - "Order.items JSON -> parseOrderItems() -> itemCount (sum of qty)"
    - "Order.total (cents) -> formatPrice() -> '$285' (no cents, deliberate)"
---

<objective>
Restructure `app/account/page.tsx` from a `"use client"` component into an async Server
Component that lists the signed-in customer's own orders, scoped by `session.user.id`
(D-07), and build the `components/account/OrderList.tsx` that renders them.

Purpose: LIVE-04 requires working order history on the deployed build. The account page
today fetches no data at all — it renders name and email only — so order history cannot
exist until the page is restructured. This is the LIST half of D-07.

Output: `components/account/OrderList.tsx` (table + responsive card fallback + 4-state
status pills + empty state), `components/account/AccountAuthActions.tsx` (the small
client island that keeps sign-in/sign-out working once the page is a Server Component),
the restructured `app/account/page.tsx`, and `tests/account-orders.test.tsx`.
</objective>

<scope_boundary>
IN scope: the order LIST on `/account` and the page restructure.

OUT of scope — belongs to plan 11-06, do not create it here:
- `app/account/orders/[id]/page.tsx` (the order DETAIL route)

`OrderList` links to `/account/orders/{id}`. That href is correct and intended; the
route it points at does not exist yet and will 404 until 11-06 lands. Do not stub the
route, and do not change the href to avoid the 404.

Also out of scope: `lib/orders.ts`, `lib/products.ts`, `lib/auth.ts`, `lib/db.ts`,
`prisma/schema.prisma`, `components/admin/OrderTable.tsx`, and `app/signin`. Read them;
do not edit them.
</scope_boundary>

<artifacts_this_phase_produces>
This plan contributes the following to Phase 11:

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| `components/account/OrderList.tsx` | Component (server-rendered, no client directive) | `app/account/page.tsx` |
| `toOrderRows()` (exported from `OrderList.tsx`) | Pure mapping helper, Prisma-free | `app/account/page.tsx`, `tests/account-orders.test.tsx` |
| `components/account/AccountAuthActions.tsx` | Client island (`signIn`/`signOut`) | `app/account/page.tsx` |
| `app/account/page.tsx` | Async Server Component route | End users; UAT of LIVE-04 |
| `tests/account-orders.test.tsx` | Vitest + jest-axe suite | CI (`npm test`) |
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-go-live/11-CONTEXT.md
@.planning/phases/11-go-live/11-UI-SPEC.md
@.planning/phases/11-go-live/11-PATTERNS.md
</context>

<reference_facts>
Facts already established by reading the codebase — trust these, do not re-derive:

- `formatPrice(cents)` in `lib/products.ts` line 201 is `` `$${(cents / 100).toFixed(0)}` `` —
  prices render as `$285`, **no cents**. This is deliberate and was re-confirmed by
  reverting an unapproved two-decimal change. Do NOT "fix" it, do NOT format prices
  inline, do NOT introduce `Intl.NumberFormat`. Call `formatPrice`.
- `parseOrderItems(itemsJson)` in `lib/orders.ts` returns `OrderItem[]` (`{slug, name,
  size, unitPrice, qty, image?}`) and returns `[]` on bad JSON instead of throwing.
  Reuse it for the item count; do not re-parse `Order.items` by hand.
- `ORDER_STATUSES` in `lib/orders.ts` is exactly `["pending","paid","fulfilled","cancelled"]`.
- Tailwind tokens (`tailwind.config.ts` lines 12-23): `cream #F4EEE4`, `cream-dark #E8DFCF`,
  `ink #1A1A1A`, `ink-soft #3A3A3A`, `sepia #A6552F`, `sepia-deep #8A4524`. The config's
  own comment records: sepia-deep on cream 6.15:1, on cream-dark **5.37:1** (raw sepia on
  cream-dark is **4.01:1** — below the 4.5:1 AA floor).
- Test harness: Vitest + jsdom + `@testing-library/react` + `jest-axe`, config at
  `vitest.config.ts` (`include: tests/**/*.test.{ts,tsx}`, `globals: true`,
  `tsconfigPaths` so `@/` aliases resolve). `tests/a11y.test.tsx` is the working
  reference for `expect.extend(toHaveNoViolations)`, `afterEach(cleanup)`, and
  `vi.mock()` of modules jsdom cannot run.
- `app/admin/orders/page.tsx` already does the exact list query + row mapping shape,
  unscoped (the owner legitimately sees all orders). Copy the shape, add the scope.
</reference_facts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build OrderList with 4-state pills, empty state, and responsive card fallback</name>

  <files>components/account/OrderList.tsx, tests/account-orders.test.tsx</files>

  <read_first>
    - `components/admin/OrderTable.tsx` — the structural analog. Copy the table shell,
      header-row classes, row borders, and the order-link `aria-label` pattern.
    - `.planning/phases/11-go-live/11-UI-SPEC.md` sections "Specific Contracts" 1, 2, 3
      and 5 — the literal source of truth for empty-state copy, pill classes, the
      `#{id.slice(-8)}` reference format, and the responsive split.
    - `.planning/phases/11-go-live/11-PATTERNS.md` section `components/account/OrderList.tsx`
      — records exactly which parts of the admin table carry over and which must not.
    - `lib/orders.ts` lines 1-60 — `ORDER_STATUSES`, `OrderItem`, `parseOrderItems`.
    - `lib/products.ts` line 201 — `formatPrice`.
    - `tests/a11y.test.tsx` lines 1-45 — the axe + cleanup + mock harness to mirror.
  </read_first>

  <behavior>
    Tests to write first, in `tests/account-orders.test.tsx`:
    - `toOrderRows([])` returns `[]`.
    - `toOrderRows` maps an order whose `items` JSON holds two lines with qty 2 and qty 3
      to `itemCount: 5`, and maps a corrupt `items` string to `itemCount: 0` (no throw).
    - `toOrderRows` formats `createdAt` as a `"en-US"` short date string and preserves
      `id`, `total`, `status` untouched.
    - `toOrderRows` sorts nothing — it preserves input order (ordering is the query's job).
    - Rendering `<OrderList orders={[]} />` shows heading text "No orders yet", body text
      "When you place an order, it'll show up here.", and a link whose accessible name is
      "Browse the collection" pointing at `/shop`.
    - The empty-state heading is an `h2` (queryable via `getByRole("heading", { level: 2 })`),
      and no `h1` is rendered by this component.
    - Rendering four orders, one per status, produces the visible text labels "Pending",
      "Paid", "Fulfilled", "Cancelled" — assert each appears at least once. This is the
      colour-independence assertion (WCAG 1.4.1): status is never colour-only.
    - The `paid` pill element's className contains `sepia-deep` and does not contain the
      raw accent token as a standalone class.
    - Each order exposes a link with accessible name `View order {last8}` whose `href` is
      `/account/orders/{full id}`, and the visible text is `#{last8}` (visible text is a
      substring of the accessible name, WCAG 2.5.3).
    - Both a `table` role and a card list are present in the DOM for the same orders (the
      dual-markup responsive strategy), and the table's wrapper carries the `sm:`-and-up
      visibility class while the card list carries the below-`sm` one.
    - `axe(container)` reports zero violations for both the empty state and the populated
      list.
  </behavior>

  <action>
    Create `components/account/OrderList.tsx`. It is a plain server-rendered component —
    do NOT add a client directive; it has no state, no effects, and no event handlers.

    Export a `Row` type: `{ id: string; itemCount: number; total: number; status: string;
    createdAt: string }`. Note there is deliberately **no email column** — the admin table
    has one because the owner needs to know whose order it is; a customer does not need
    their own email repeated back at them (UI-SPEC column set is `Order | Date | Items |
    Total | Status`).

    Export a pure helper `toOrderRows(orders)` that accepts the minimal shape
    `{ id: string; items: string; total: number; status: string; createdAt: Date }[]` and
    returns `Row[]`. It must stay Prisma-import-free so it is unit-testable without a DB,
    mirroring the header comment convention on `lib/orders.ts`. Inside it: derive
    `itemCount` as `parseOrderItems(o.items).reduce((sum, i) => sum + i.qty, 0)` and
    `createdAt` as `o.createdAt.toLocaleDateString("en-US", { year: "numeric", month:
    "short", day: "numeric" })` — identical to `app/admin/orders/page.tsx` lines 13-23.

    Status presentation: build a lookup from status string to `{ label, className }` using
    UI-SPEC "Specific Contracts" section 2 **verbatim** — `pending` neutral, `paid`
    accent-tinted using the deep accent variant, `fulfilled` solid ink on cream,
    `cancelled` the existing destructive `#9B2C2C` tint. PATTERNS.md line 148 explicitly
    warns that the admin `pillClass()` must NOT be copied verbatim: it uses the
    un-deepened accent on a tinted background, which is only AA-safe because admin rows
    sit on plain cream. Our `paid` pill also renders inside a cream-dark mobile card,
    where the un-deepened accent measures 4.01:1 — below the AA floor. Use the deep
    variant everywhere in this file. For an unrecognised status value (the column is a
    `String`, so treat it defensively), fall back to the neutral classes and render the
    raw value capitalised rather than rendering nothing. Every pill renders its text
    label; colour is decoration on top of text, never a substitute for it.

    Pill markup shape: an inline-block span, rounded-sm, `px-2 py-1`, `text-xs`,
    `font-medium`, plus the per-status classes.

    Empty state (orders array is empty): return the cream-dark card described by UI-SPEC
    section 1 — `mt-10 rounded-sm bg-cream-dark px-6 py-16 text-center`, an `h2` in
    `font-serif text-xl text-ink` reading "No orders yet", a `mt-2 text-sm text-ink-soft`
    paragraph reading "When you place an order, it'll show up here.", and a `.btn-primary`
    `next/link` to `/shop` reading "Browse the collection". That CTA string is the
    established sitewide empty-state copy (`/cart`, `/checkout`, `/wishlist`,
    `CartDrawer`) — do not invent a variant. This state is the most common first-visit
    state for a signed-in customer and must read as intentional, not as a failed render.

    Populated state: render an `sr-only` `h2` reading "Your orders" as the first child,
    then both markup structures. The page's `h1` is "Hello, {name}", so a component that
    jumped straight to a table would leave the section unlabelled and, once any lower
    heading appears, skip a level. The `/shop` route needed exactly this treatment in
    Phase 10 for the same reason. Give the table `aria-labelledby` pointing at that
    heading's id.

    Desktop structure (640px and up): mirror `components/admin/OrderTable.tsx` lines 36-79
    — `w-full text-left text-sm` table, `thead` with `border-b border-cream-dark text-xs
    uppercase tracking-[0.15em] text-ink-soft`, `th` cells `py-3 pr-4 font-medium`, `tbody`
    rows `border-b border-cream-dark/60`. Columns: Order, Date, Items, Total, Status. The
    order cell is a `next/link` to `/account/orders/${o.id}` with
    ``aria-label={`View order ${o.id.slice(-8)}`}`` and visible text ``#{o.id.slice(-8)}``,
    styled with the deep accent and an ink hover. Items and Total cells use `tabular-nums`;
    Total calls `formatPrice(o.total)`. The order reference itself gets no `tabular-nums` —
    it is an alphanumeric cuid fragment, not a number.

    Mobile structure (below 640px): per UI-SPEC section 5, do NOT render the table and do
    NOT wrap it in a horizontally scrolling container — horizontal page scroll is
    explicitly disallowed. Instead render a `space-y-4` stack of cream-dark cards, one per
    order, each card being a single `next/link` to the same href with the same
    `aria-label`, `min-h-[44px]` for the touch target, `rounded-sm bg-cream-dark p-4`.
    Card row 1: the `#{last8}` reference on the left in the deep accent, the date on the
    right in `text-ink-soft`. Card row 2: the status pill on the left, `formatPrice(total)`
    on the right in `tabular-nums`. Swap between the two structures with Tailwind
    responsive visibility prefixes (`hidden sm:block` on the table wrapper, `sm:hidden` on
    the card stack) — a semantic table cannot reflow into cards with CSS alone.

    Because both structures are in the DOM, the mobile card list must not duplicate the
    accessible table content for screen readers in a confusing way: keep the `sr-only`
    heading outside both, and let each structure carry its own links. Verify with axe.

    Then write `tests/account-orders.test.tsx` covering the `<behavior>` list, following
    the harness in `tests/a11y.test.tsx`: `expect.extend(toHaveNoViolations)`,
    `afterEach(cleanup)`. This component imports nothing that jsdom cannot handle, so no
    module mocks are needed for this task's blocks.
  </action>

  <verify>
    <automated>npx vitest run tests/account-orders.test.tsx</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -vE '^\s*(//|\*|/\*)' components/account/OrderList.tsx | grep -cE '(text|bg)-sepia[^-]' | grep -qx 0</automated>
    <automated>grep -vE '^\s*(//|\*|/\*)' components/account/OrderList.tsx | grep -c 'overflow-x' | grep -qx 0</automated>
  </verify>

  <acceptance_criteria>
    - [ ] `components/account/OrderList.tsx` exists and exports a default component plus `toOrderRows`.
    - [ ] `npx vitest run tests/account-orders.test.tsx` exits 0 with every `<behavior>` assertion present.
    - [ ] `npx tsc --noEmit` exits 0.
    - [ ] `grep -vE '^\s*(//|\*|/\*)' components/account/OrderList.tsx | grep -cE '(text|bg)-sepia[^-]'` returns `0` (no un-deepened accent class anywhere in the file).
    - [ ] `grep -vE '^\s*(//|\*|/\*)' components/account/OrderList.tsx | grep -c 'overflow-x'` returns `0` (no horizontal-scroll escape hatch).
    - [ ] `grep -c 'hidden sm:block' components/account/OrderList.tsx` is at least 1 and `grep -c 'sm:hidden' components/account/OrderList.tsx` is at least 1.
    - [ ] `grep -c 'sepia-deep' components/account/OrderList.tsx` is at least 1.
    - [ ] The file contains no client directive: `grep -c "use client" components/account/OrderList.tsx` returns `0`.
    - [ ] The axe assertions pass for both the empty and populated renders.
  </acceptance_criteria>

  <done>
    OrderList renders a labelled table on desktop and a card stack on mobile, with four
    text-labelled status pills that never rely on colour alone, an AA-safe paid pill on
    cream-dark, and a branded empty state — all covered by passing Vitest + axe assertions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract the sign-in / sign-out client island</name>

  <files>components/account/AccountAuthActions.tsx, tests/account-orders.test.tsx</files>

  <read_first>
    - `app/account/page.tsx` lines 1-47 — the current component. Lines 15-23 are the
      signed-out branch, lines 34-44 are the signed-in action row. Both move here.
    - `.planning/phases/11-go-live/11-PATTERNS.md` section `app/account/page.tsx`
      (restructure) — records that only the auth actions need to stay client-side.
    - `tests/a11y.test.tsx` lines 1-20 — the `vi.mock()` idiom for modules jsdom cannot run.
  </read_first>

  <behavior>
    Added to `tests/account-orders.test.tsx`, with `next-auth/react` mocked:
    - `<AccountSignedOut />` renders an `h1` reading "You're signed out" and a button
      labelled "Sign in"; clicking the button calls the mocked `signIn` once.
    - `<AccountSignOutButton />` renders a button labelled "Sign out"; clicking it calls
      the mocked `signOut` once with `{ callbackUrl: "/" }`.
    - `axe()` reports zero violations for both.
  </behavior>

  <action>
    Create `components/account/AccountAuthActions.tsx`. Prefix the file with the Next.js
    client-component directive — the same single-token first line that
    `components/CartDrawer.tsx` and the current account page already use. This file is the
    only client code left on the `/account` route, and it exists solely because
    `signIn()`/`signOut()` from `next-auth/react` are browser-side calls that a Server
    Component cannot invoke.

    Export two named components:

    `AccountSignedOut` — copy the current signed-out branch JSX (`app/account/page.tsx`
    lines 16-23) verbatim, including the `container-x flex min-h-[50vh] flex-col
    items-center justify-center gap-5 py-20 text-center` wrapper, the `font-serif text-4xl
    font-black` h1 reading "You're signed out" (keep the `&apos;` entity), and the
    `.btn-primary` button calling `signIn()`. Do not restyle it and do not change the copy
    — this branch is already shipped and approved; it is being relocated, not redesigned.
    Render it as a fragment or a `div`, not a `<main>`; the page owns the landmark.

    `AccountSignOutButton` — the `.btn-outline` button from lines 38-43 calling
    `signOut({ callbackUrl: "/" })`. Nothing else.

    Do not put the "Continue shopping" link in this file — it is a plain `next/link` with
    no interactivity and belongs in the Server Component, so it stays server-rendered.

    Then extend `tests/account-orders.test.tsx` with a describe block for these two,
    mocking `next-auth/react` with `vi.mock("next-auth/react", () => ({ signIn: vi.fn(),
    signOut: vi.fn() }))`. Import the mocked functions and assert the call counts and
    arguments. Note `vi.mock` is hoisted file-wide by Vitest, which is harmless for
    Task 1's blocks since `OrderList` does not import `next-auth/react`.
  </action>

  <verify>
    <automated>npx vitest run tests/account-orders.test.tsx</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - [ ] `components/account/AccountAuthActions.tsx` exists and exports `AccountSignedOut` and `AccountSignOutButton`.
    - [ ] The file's first line is the client-component directive: `head -1 components/account/AccountAuthActions.tsx | grep -c 'use client'` returns `1`.
    - [ ] `grep -c 'next-auth/react' components/account/AccountAuthActions.tsx` is at least 1.
    - [ ] `npx vitest run tests/account-orders.test.tsx` exits 0 including the two new interaction assertions.
    - [ ] `npx tsc --noEmit` exits 0.
    - [ ] The signed-out heading copy is byte-identical to the current page's (no reword, no restyle).
  </acceptance_criteria>

  <done>
    Sign-in and sign-out remain fully functional as a minimal client island, leaving the
    rest of `/account` free to become a Server Component.
  </done>
</task>

<task type="auto">
  <name>Task 3: Convert app/account/page.tsx to a session-scoped async Server Component</name>

  <files>app/account/page.tsx</files>

  <read_first>
    - `app/account/page.tsx` (all 47 lines) — the file being replaced.
    - `app/admin/orders/page.tsx` (all 25 lines) — the list-query + row-mapping shape to
      copy, plus its comment explaining why the admin query is deliberately unscoped.
    - `lib/auth.ts` — `authOptions`, and how `session.user.id` is populated.
    - `lib/admin.ts` `requireOwner()` — the house authorization idiom: redirect anonymous
      users, never reveal existence to authenticated-but-unauthorized ones.
    - `.planning/phases/11-go-live/11-PATTERNS.md` section `app/account/page.tsx` (restructure).
  </read_first>

  <action>
    Rewrite `app/account/page.tsx` as an async Server Component. Delete the client
    directive on line 1 and the three client-hook imports from `next-auth/react`; the
    session now comes from `getServerSession(authOptions)`, the same server-side call
    `lib/admin.ts` and `app/api/checkout/route.ts` already use. Deleting the client hook
    also deletes the `status === "loading"` branch — a Server Component has no loading
    state to render, it simply awaits.

    Add `export const dynamic = "force-dynamic";`. `getServerSession` reads cookies so
    Next should already treat the route as dynamic, but this route renders one customer's
    private orders and an accidental static or shared cache entry would serve one
    customer's order list to another. Make it explicit rather than inferred.

    Signed-out branch: if there is no `session?.user`, return the existing `<main
    className="container-x ...">` landmark wrapping `<AccountSignedOut />` from Task 2.
    Same visual result as today.

    Extract the user id as `const userId = (session.user as { id?: string }).id;`.

    **Critical — guard before querying.** If `userId` is falsy, do NOT fall through to the
    query. Prisma silently drops `undefined` filter values, so
    `findMany({ where: { userId: undefined } })` returns **every order in the database**,
    which would hand one visitor the entire customer base's order history. Treat a missing
    id as "no orders": either `redirect("/signin?callbackUrl=/account")` or render the
    empty state with an empty array. Never let an unscoped `findMany` run on this route.
    Leave a short comment on the guard saying why it exists, so a future refactor does not
    delete it as redundant.

    Query, mirroring `app/admin/orders/page.tsx` lines 8-11 with the scope added:
    `await prisma.order.findMany({ where: { userId }, orderBy: { createdAt: "desc" } })`.
    The `where` clause is the entire security boundary of this page (D-07) — it is not an
    optimisation and must not be moved into client-side filtering. Map the result through
    `toOrderRows()` from Task 1.

    Keep the greeting block exactly as-is (current lines 27-32): the `.eyebrow` "Account"
    paragraph, the `mt-2 font-serif text-5xl font-black` h1 reading
    `Hello, {session.user.name?.split(" ")[0] ?? "friend"}`, and the `mt-3 text-ink-soft`
    email line. UI-SPEC section 1 is explicit: the order list goes *below* the greeting and
    does not replace it. This h1 is the page's only h1 — which is why OrderList starts at
    h2.

    Keep the existing `mt-10 flex flex-wrap gap-3` action row: the "Continue shopping"
    `.btn-primary` link stays a plain server-rendered `next/link`, and the sign-out button
    becomes `<AccountSignOutButton />` from Task 2.

    Render `<OrderList orders={rows} />` after the greeting. Place it above or below the
    action row at your discretion; UI-SPEC only requires it be below the greeting. Prefer
    below the action row so the primary CTA stays near the greeting on mobile.

    Do not add pagination, filtering, or sorting controls — none are in D-07 and none are
    in UI-SPEC. Newest-first is the whole ordering contract.

    Do not create `app/account/orders/[id]/page.tsx`. Plan 11-06 owns it.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm test</automated>
    <automated>npx next build 2>&1 | tail -20</automated>
    <automated>grep -vE '^\s*(//|\*|/\*)' app/account/page.tsx | grep -c 'use client' | grep -qx 0</automated>
    <automated>grep -c 'getServerSession' app/account/page.tsx</automated>
    <automated>grep -c 'where: { userId }' app/account/page.tsx</automated>
  </verify>

  <acceptance_criteria>
    - [ ] `grep -vE '^\s*(//|\*|/\*)' app/account/page.tsx | grep -c 'use client'` returns `0`.
    - [ ] `grep -c 'getServerSession' app/account/page.tsx` is at least 1.
    - [ ] `grep -c 'where: { userId }' app/account/page.tsx` is exactly 1, and `grep -c 'findMany' app/account/page.tsx` is exactly 1 — the only order query on this route is the scoped one.
    - [ ] A falsy-`userId` guard appears textually before the `findMany` call (verify by reading the file top-to-bottom; the guard must `return`/`redirect`, not merely log).
    - [ ] `grep -c 'force-dynamic' app/account/page.tsx` is at least 1.
    - [ ] The h1 still reads `Hello, {firstName}` and the `.eyebrow` "Account" line is unchanged.
    - [ ] `npx tsc --noEmit` exits 0.
    - [ ] `npm test` exits 0 — the full suite, including the Phase 10 a11y suite, still passes with zero regressions.
    - [ ] `npx next build` completes without error and does not report `/account` as statically prerendered.
    - [ ] `app/account/orders/[id]/page.tsx` does NOT exist (11-06 owns it): `ls app/account/orders 2>/dev/null | wc -l` returns `0`.
  </acceptance_criteria>

  <done>
    `/account` is an async Server Component that fetches and renders only the signed-in
    customer's orders, newest first, with sign-in/sign-out still working, the greeting
    intact, and the full test suite green.
  </done>
</task>

</tasks>

<threat_model>
ASVS Level 1. Policy: block on `high` or above — a `high` finding left unmitigated fails
the plan.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `/account` Server Component | An unauthenticated or differently-authenticated visitor requests a page that renders private order data. The session cookie is the only credential. |
| session object → Prisma query | `session.user.id` becomes a `where` clause. If it is absent or attacker-influenced, the query's scope collapses. |
| Next.js render cache → response | A cached render of a per-user page can be served to a different user. |
| `Order.items` JSON → rendered DOM | Stored data written at checkout, re-read here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-03-01 | Information Disclosure | `app/account/page.tsx` order query | critical | mitigate | Query is `findMany({ where: { userId } })`, scoped server-side (Task 3). Acceptance criteria assert exactly one `findMany` and exactly one `where: { userId }` in the file, so no unscoped sibling query can slip in. |
| T-11-03-02 | Information Disclosure | Prisma `undefined`-filter collapse | critical | mitigate | Explicit falsy-`userId` guard that returns/redirects **before** `findMany` (Task 3). Without it, `where: { userId: undefined }` returns every order in the database. Called out inline in the file with a comment so it survives refactors. |
| T-11-03-03 | Spoofing | client-supplied identity | high | mitigate | The user id is read only from `getServerSession(authOptions)` server-side. No id is accepted from a query string, header, prop, or client component. `/account` takes no route params at all. |
| T-11-03-04 | Information Disclosure | render caching | high | mitigate | `export const dynamic = "force-dynamic"` on the route (Task 3), plus a build-output check that `/account` is not listed as statically prerendered. |
| T-11-03-05 | Information Disclosure | order id in link href | low | accept | `/account/orders/{id}` exposes the full cuid in the customer's own DOM — it is their own order. Plan 11-06 independently re-scopes the detail route by `id` AND `userId` per UI-SPEC section 4, so possessing an id grants nothing. |
| T-11-03-06 | Tampering | `Order.items` JSON | low | mitigate | Rendered through `parseOrderItems()`, which returns `[]` on malformed input rather than throwing. Only the derived integer `itemCount` reaches this plan's DOM; no raw item strings are rendered by the list. |
| T-11-03-07 | Denial of Service | unbounded `findMany` | low | accept | A single customer's own order count is naturally bounded during UAT and early production. Pagination is not in D-07 or UI-SPEC; revisit if order volume per customer grows. |
| T-11-03-SC | Tampering | supply chain | n/a | accept | This plan installs **no packages**. `next-auth`, `@prisma/client`, `vitest`, `jest-axe`, and `@testing-library/react` are all existing dependencies. No `npm install` step exists here, so RESEARCH.md's package legitimacy gate does not apply. If any task appears to need a new dependency, stop and escalate rather than installing. |
</threat_model>

<verification>
Run from the project root after all three tasks:

1. `npx tsc --noEmit` — exits 0.
2. `npm test` — full Vitest suite exits 0, including the Phase 10 a11y suite. Zero
   regressions is the bar; Phase 10 reached Lighthouse a11y 100 on every route and this
   plan must not cost a single point.
3. `npx next build` — completes; `/account` is not reported as statically prerendered.
4. Scoping proof (the security assertion of this plan): with two seeded customers each
   holding orders, sign in as customer A and confirm the list contains only A's orders.
   Then read `app/account/page.tsx` and confirm there is exactly one `findMany`, it
   carries `where: { userId }`, and a falsy-`userId` guard precedes it.
5. Empty state: sign in as a customer with zero orders and confirm the cream-dark card
   renders with "No orders yet", the body line, and a working "Browse the collection" CTA
   — not a blank region.
6. Responsive: at a 375px viewport, confirm the table is not rendered, cards are, and the
   page does not scroll horizontally (`document.documentElement.scrollWidth` equals
   `clientWidth`).
7. Colour independence: with a greyscale filter applied, all four statuses remain
   distinguishable by their text labels.
8. Prices render with no cents (`$285`, not `$285.00`) — this is `formatPrice`'s
   deliberate behaviour, not a defect.
</verification>

<success_criteria>
- LIVE-04's list half is delivered: a signed-in customer sees their own orders on
  `/account`, newest first, with reference, date, item count, total, and status.
- The order query is scoped to `session.user.id` and cannot run unscoped (D-07).
- Sign-in and sign-out still work via a minimal client island; the greeting block is
  unchanged.
- The empty state is branded and intentional-looking.
- Status is text-labelled, the paid pill uses the AA-safe deep accent, and heading order
  runs h1 → h2 with no skips.
- Mobile degrades to cards with no horizontal page scroll.
- `npx tsc --noEmit`, `npm test`, and `npx next build` all pass.
- `app/account/orders/[id]/page.tsx` is untouched and uncreated — 11-06 owns it.
</success_criteria>

<output>
Create `.planning/phases/11-go-live/11-03-SUMMARY.md` when done.

Record in the summary: whether the sign-out island ended up folded into
`AccountAuthActions.tsx` as planned, any deviation from UI-SPEC's pill classes and why,
the observed `npm test` count before and after, and confirmation that the falsy-`userId`
guard is present with its explanatory comment.
</output>
