---
phase: 10-trust-polish
plan: 08
type: execute
wave: 4
depends_on: []
files_modified:
  - app/shipping/page.tsx
  - app/returns/page.tsx
  - components/Footer.tsx
  - app/product/[slug]/page.tsx
autonomous: true
requirements: [TRST-03]

must_haves:
  truths:
    - "Visiting /shipping renders a static shipping policy page inside the storefront chrome with its own <title> (D-11, D-12)."
    - "Visiting /returns renders a static returns policy page inside the storefront chrome with its own <title> (D-11, D-12)."
    - "The Footer 'The House' column contains working links to /shipping and /returns, site-wide (D-13)."
    - "On the PDP, the 'Free shipping over $200' and '30-day returns' bullet lines are clickable links to /shipping and /returns (D-13)."
  artifacts:
    - app/shipping/page.tsx
    - app/returns/page.tsx
  key_links:
    - "components/Footer.tsx -> /shipping, /returns (Link)"
    - "app/product/[slug]/page.tsx -> /shipping, /returns (Link)"
---

<objective>
Ship the shipping & returns trust surface (TRST-03): two separate, statically-rendered
policy routes — `/shipping` and `/returns` — each owning its own `metadata` export and
Claude-drafted on-brand PLACEHOLDER copy (owner edits real terms before go-live), then wire
real `<Link>`s to both from the site-wide Footer and from the PDP where purchase questions
arise.

Purpose: A shopper (and search engines) can find dedicated, linked shipping and returns
information — the last trust page before go-live. Implements D-11 (two separate pages),
D-12 (static Claude-drafted placeholder copy), D-13 (linked from Footer + PDP), following
the editorial content-page contract in UI-SPEC §5. Not added to checkout this phase.

Output: `app/shipping/page.tsx`, `app/returns/page.tsx`, and link wiring in
`components/Footer.tsx` + `app/product/[slug]/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-trust-polish/10-UI-SPEC.md
@.planning/phases/10-trust-polish/10-CONTEXT.md
@app/not-found.tsx
@app/layout.tsx
@app/globals.css
@components/Footer.tsx
@app/product/[slug]/page.tsx
</context>

<interface_context>
Reused, do not re-implement:
- Layout/type: policy pages are Next.js App Router **Server Components** (default) — no
  `"use client"`, no data fetching, no `export const dynamic`. They render statically.
  Follow `app/not-found.tsx` as the closest static-page precedent (`container-x`, eyebrow,
  `font-serif ... font-black` h1). Do NOT copy the client-component form of `app/cart` /
  `app/checkout` — those are `"use client"` and cannot export `metadata`.
- Metadata: import the `Metadata` type from `next`; export `const metadata: Metadata`.
  Title format follows the root layout em-dash convention in `app/layout.tsx`
  (`"Nostalgia — Wear the memory"`). This plan OWNS the base metadata for both routes;
  SEO plan 10-11 adds only `app/sitemap.ts` / `app/robots.ts` entries and must not
  redeclare these exports.
- Utility classes (from `app/globals.css`): `.container-x`, `.eyebrow` (sepia uppercase),
  `.link-underline`, `.btn-primary`. Do not introduce new CSS.
- `next/link` `Link` is ALREADY imported in both `components/Footer.tsx` and
  `app/product/[slug]/page.tsx` — do not add a duplicate import.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Create app/shipping/page.tsx (static page + own metadata + placeholder copy)</name>
  <files>app/shipping/page.tsx</files>
  <read_first>
    - .planning/phases/10-trust-polish/10-UI-SPEC.md (§5 Policy pages — heading hierarchy, measure, section rhythm)
    - .planning/phases/10-trust-polish/10-CONTEXT.md (D-11, D-12)
    - app/not-found.tsx (static Server Component + container-x pattern)
    - app/layout.tsx (Metadata export + title format)
    - app/globals.css (.container-x, .eyebrow utilities)
  </read_first>
  <action>
    Create `app/shipping/page.tsx` as a statically-rendered Server Component: no
    `"use client"`, no `export const dynamic`, no data fetching (keeping the route static
    also serves PERF-01). Import the `Metadata` type from `next` and export
    `const metadata: Metadata` with `title` set to "Shipping — Nostalgia" and a one-sentence
    `description` summarising the shipping policy, matching the em-dash title format used in
    `app/layout.tsx`.

    Render `<main className="container-x py-16">` wrapping a single
    `<div className="max-w-2xl">` (the ~65ch editorial measure required by UI-SPEC §5).
    Inside, in order: `<p className="eyebrow">Customer Care</p>`; then
    `<h1 className="mt-3 font-serif text-5xl font-black">Shipping</h1>`; then policy
    sections, each an `<h2 className="font-serif text-2xl font-black mt-12">` heading
    followed by one or more `<p className="mt-4 text-ink-soft leading-relaxed">` paragraphs;
    use `<h3 className="text-lg font-medium mt-6">` sparingly only for the Domestic /
    International split. Cover these sections in this order: Processing &amp; dispatch times;
    Domestic shipping (rates + estimated delivery windows — state free shipping over $200 so
    it stays consistent with the PDP bullet); International shipping (a duties/customs note);
    Order tracking.

    Draft the prose yourself as on-brand PLACEHOLDER copy per D-12: the storefront's quiet-
    luxury editorial voice (calm, declarative, no exclamation points, no urgency copy),
    plausible but generic — containing NO real street addresses, phone numbers, PII, or
    binding legal terms. Place a leading JSX comment noting the copy is placeholder for the
    owner to replace before go-live (reference D-12) at the top of the returned markup. Add
    no interactive elements, forms, or client hooks.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q 'export const metadata' app/shipping/page.tsx && grep -q 'Customer Care' app/shipping/page.tsx && grep -q 'container-x py-16' app/shipping/page.tsx</automated>
    <manual>After `npm run dev -- -p 3002`, load http://localhost:3002/shipping — page renders inside the storefront chrome (Nav/Footer/grain), the browser tab shows "Shipping — Nostalgia", and the editorial hierarchy (eyebrow → h1 → h2 sections) reads on-brand.</manual>
  </verify>
  <acceptance_criteria>
    - `app/shipping/page.tsx` exists as a Server Component (no `"use client"`, no `dynamic` export) and `npx tsc --noEmit` passes.
    - It exports `const metadata: Metadata` with `title: "Shipping — Nostalgia"` and a non-empty `description`.
    - Markup uses `container-x py-16` + a `max-w-2xl` measure, an `.eyebrow` "Customer Care", a `font-serif text-5xl font-black` h1 "Shipping", and `h2 font-serif text-2xl font-black mt-12` section headings per UI-SPEC §5.
    - Copy is Claude-drafted placeholder (D-12): quiet-luxury voice, no PII/addresses/legal terms, with a leading comment marking it owner-replaceable.
  </acceptance_criteria>
  <done>`/shipping` renders as a static Server Component with title "Shipping — Nostalgia" and on-brand placeholder copy; `npx tsc --noEmit` clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create app/returns/page.tsx (static page + own metadata + placeholder copy)</name>
  <files>app/returns/page.tsx</files>
  <read_first>
    - .planning/phases/10-trust-polish/10-UI-SPEC.md (§5 Policy pages contract)
    - .planning/phases/10-trust-polish/10-CONTEXT.md (D-11, D-12)
    - app/shipping/page.tsx (Task 1 — mirror its structure exactly)
    - app/layout.tsx (Metadata export + title format)
  </read_first>
  <action>
    Create `app/returns/page.tsx` mirroring the Task 1 structure exactly (same static
    Server Component form, same UI-SPEC §5 layout: `container-x py-16` main wrapping a
    `max-w-2xl` div; `.eyebrow` "Customer Care"; `<h1 className="mt-3 font-serif text-5xl
    font-black">Returns</h1>`; `h2 font-serif text-2xl font-black mt-12` sections with
    `mt-4 text-ink-soft leading-relaxed` paragraphs). Export `const metadata: Metadata` with
    `title` "Returns — Nostalgia" and a one-sentence `description`.

    Cover these sections in order: an overview/promise paragraph; Return window (state a
    30-day window so it stays consistent with the PDP "30-day returns" bullet); Condition
    &amp; eligibility; How to start a return; Refunds (timing + method); Exchanges. Draft
    on-brand PLACEHOLDER copy per D-12 in the same quiet-luxury editorial voice — plausible,
    generic, no PII/addresses/binding legal terms — with the same leading placeholder
    comment referencing D-12. No interactive elements, forms, or client hooks.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q 'export const metadata' app/returns/page.tsx && grep -q 'Customer Care' app/returns/page.tsx && grep -q 'container-x py-16' app/returns/page.tsx</automated>
    <manual>After `npm run dev -- -p 3002`, load http://localhost:3002/returns — page renders inside the storefront chrome, the tab shows "Returns — Nostalgia", and the editorial hierarchy matches the shipping page.</manual>
  </verify>
  <acceptance_criteria>
    - `app/returns/page.tsx` exists as a Server Component (no `"use client"`, no `dynamic` export) and `npx tsc --noEmit` passes.
    - It exports `const metadata: Metadata` with `title: "Returns — Nostalgia"` and a non-empty `description`.
    - Layout mirrors the shipping page contract (container-x py-16, max-w-2xl, eyebrow "Customer Care", `font-serif text-5xl font-black` h1 "Returns", `mt-12` h2 sections).
    - Copy is Claude-drafted placeholder (D-12) in the storefront voice, states a 30-day return window, and carries the owner-replaceable comment.
  </acceptance_criteria>
  <done>`/returns` renders as a static Server Component with title "Returns — Nostalgia" and on-brand placeholder copy mirroring the shipping page; `npx tsc --noEmit` clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire /shipping + /returns links from the Footer and the PDP</name>
  <files>components/Footer.tsx, app/product/[slug]/page.tsx</files>
  <read_first>
    - components/Footer.tsx (the "The House" column ul — lines ~39-47)
    - app/product/[slug]/page.tsx (the bullet ul below AddToCart — lines ~74-78)
    - .planning/phases/10-trust-polish/10-UI-SPEC.md (§5 "Linked from")
    - .planning/phases/10-trust-polish/10-CONTEXT.md (D-13)
  </read_first>
  <action>
    In `components/Footer.tsx`, inside the "The House" column's `<ul>` (the one preceded by
    `<p className="eyebrow mb-4">The House</p>`), add two new `<li>` items containing real
    links: one `<Link href="/shipping" className="link-underline">Shipping</Link>` and one
    `<Link href="/returns" className="link-underline">Returns</Link>`. Leave the four
    existing plain-text placeholder items (Our Story, Journal, Stockists, Contact)
    unchanged — they stay out of scope this phase per UI-SPEC §5. `Link` is already imported;
    do not add a duplicate import.

    In `app/product/[slug]/page.tsx`, in the `<ul className="mt-8 space-y-1 ...">` list
    below `<AddToCart>` (currently three `<li>` bullets), convert ONLY the shipping and
    returns lines into links while preserving the exact copy, the leading "— " prefix, and
    the existing item order: wrap the "Free shipping over $200" text in
    `<Link href="/shipping" className="link-underline">` and the "30-day returns" text in
    `<Link href="/returns" className="link-underline">`. Keep "— Made in limited runs" as
    plain text. Add no new list items and no new UI element — only make existing text
    actionable per D-13. `Link` is already imported and `export const dynamic =
    "force-dynamic"` must remain untouched.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q 'href="/shipping"' components/Footer.tsx && grep -q 'href="/returns"' components/Footer.tsx && grep -q 'href="/shipping"' 'app/product/[slug]/page.tsx' && grep -q 'href="/returns"' 'app/product/[slug]/page.tsx'</automated>
    <manual>After `npm run dev -- -p 3002`: the Footer "The House" column shows working Shipping and Returns links on every page; on a PDP, clicking "Free shipping over $200" navigates to /shipping and "30-day returns" navigates to /returns, with "Made in limited runs" still plain text.</manual>
  </verify>
  <acceptance_criteria>
    - `components/Footer.tsx` "The House" column contains real `<Link>`s to `/shipping` and `/returns` (`link-underline`); the four original placeholder items remain unchanged; `npx tsc --noEmit` passes.
    - `app/product/[slug]/page.tsx` renders "Free shipping over $200" as a link to `/shipping` and "30-day returns" as a link to `/returns`, preserving exact copy, "— " prefix, and position; "Made in limited runs" stays plain text; `force-dynamic` untouched.
    - No duplicate `Link` imports introduced in either file.
  </acceptance_criteria>
  <done>Footer (site-wide) and the PDP both expose working `<Link>`s to `/shipping` and `/returns`; existing copy/order preserved; `npx tsc --noEmit` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous visitor / crawler → static policy route | Public reads server-rendered `/shipping` and `/returns`; no request-supplied input is processed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-08-01 | Information Disclosure | placeholder policy copy | low | mitigate | Copy is generic, Claude-drafted placeholder (D-12) — contains no real addresses, PII, phone numbers, or binding legal terms; marked in-file as owner-replaceable before go-live. |
| T-10-08-02 | Tampering | policy page render | low | accept | Content is static literal JSX with no `dangerouslySetInnerHTML`, no user/DB input, and no client hooks — no injection surface. |
| T-10-08-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages; the RESEARCH.md Package Legitimacy Gate does not apply. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean across the project (dev-runnable, `tsc` clean constraint).
- `/shipping` and `/returns` each render as static Server Components with their own `<title>`.
- Footer (site-wide) and the PDP both expose working `<Link>`s to `/shipping` and `/returns`.
- No new CSS utilities added; all styling reuses `app/globals.css` classes.
- Full suite unaffected: `npx vitest run` stays green (no logic changed).
</verification>

<success_criteria>
- TRST-03 satisfied: dedicated shipping & returns information pages exist and are linked.
- Both routes carry their own metadata (feeds SEO-01 downstream; owned here).
- Placeholder copy is on-brand and clearly owner-replaceable (D-12).
- Links reachable from the footer on every page and from the PDP purchase area (D-13).
- Not added to checkout this phase (per D-13 scope fence).
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Notes |
|----------|------|-------|
| `app/shipping/page.tsx` | new route | Static Server Component; own `metadata`; placeholder shipping copy (D-11, D-12). |
| `app/returns/page.tsx` | new route | Static Server Component; own `metadata`; placeholder returns copy (D-11, D-12). |
| `components/Footer.tsx` | modified | Adds real `<Link>`s to `/shipping` + `/returns` in "The House" column (D-13). |
| `app/product/[slug]/page.tsx` | modified | Converts the shipping + returns PDP bullets into links (D-13). |

<output>
Create `.planning/phases/10-trust-polish/10-08-SUMMARY.md` when done.
</output>
