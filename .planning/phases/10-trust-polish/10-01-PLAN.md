---
phase: 10-trust-polish
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tests/reviews.test.ts
  - tests/size-guides.test.ts
  - tests/seo.test.ts
  - tests/analytics.test.ts
  - tests/a11y.test.tsx
autonomous: true
requirements: [TRST-01, TRST-02, SEO-01, ANLY-01, PERF-01]

must_haves:
  truths:
    - "jest-axe is installed as a devDependency and importable under Vitest (jsdom)"
    - "Five Wave-0 test files exist and fail RED-by-design, pinning the lib/component API contracts every later Phase 10 plan must satisfy"
    - "The pre-existing suite (orders, catalog, admin, wishlist, smoke) still passes unchanged"
  artifacts:
    - "tests/reviews.test.ts"
    - "tests/size-guides.test.ts"
    - "tests/seo.test.ts"
    - "tests/analytics.test.ts"
    - "tests/a11y.test.tsx"
    - "jest-axe entry in package.json devDependencies"
  key_links:
    - "reviews/size-guides/seo/analytics tests import @/lib/{reviews,size-guides,seo,analytics} — the exact contract 10-03 / 10-07 / 10-11 / 10-09 will implement to turn each file green"
    - "tests/a11y.test.tsx dynamically imports @/components/{ReviewForm,SizeGuideModal,ConsentBanner} (default exports) so each describe block can green independently in 10-05 / 10-07 / 10-09"
    - "reviews.test.ts mocks @/lib/db (prisma); analytics.test.ts mocks @vercel/analytics (track) — no live DB, no live analytics network"
---

<objective>
Land the Phase 10 test foundation: install the one missing test dependency (`jest-axe`)
and author the five Wave-0 test files as **failing RED scaffolds** that encode the
pure-logic and accessibility contracts for the whole phase.

This plan is deliberately RED. The five new files target modules and components that do
not exist yet (`lib/reviews.ts`, `lib/size-guides.ts`, `lib/seo.ts`, `lib/analytics.ts`,
`components/ReviewForm`, `components/SizeGuideModal`, `components/ConsentBanner`). The
executor of THIS plan must NOT create those modules/components — later plans (10-03,
10-05, 10-07, 10-09, 10-11) turn the scaffolds green. Creating the targets here would
break the phase's TDD ordering.

Purpose: Give every downstream Phase 10 plan a concrete, executable definition of "done"
before any implementation is written — the tests ARE the spec (Nyquist Wave-0 gaps from
10-VALIDATION.md).
Output: `jest-axe` in devDependencies + five RED test files pinning the phase's testable API.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-trust-polish/10-RESEARCH.md
@.planning/phases/10-trust-polish/10-UI-SPEC.md
@.planning/phases/10-trust-polish/10-VALIDATION.md

# Test-convention exemplars the new files must mirror (import style, describe/it shape):
@tests/orders.test.ts
@tests/catalog.test.ts
# Pure helper reused by the reviews eligibility contract:
@lib/orders.ts
# Prisma singleton the reviews test mocks:
@lib/db.ts
# Vitest config (jsdom, globals, "@" alias) — no changes needed:
@vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install jest-axe dev dependency</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - `.planning/phases/10-trust-polish/10-RESEARCH.md` §"Package Legitimacy Audit" and §"Standard Stack" (jest-axe 10.0.0 verdict = OK / Approved; dev/test-only; works with Vitest via `expect.extend`).
    - `package.json` (current devDependencies — Vitest, @testing-library/react, jsdom already present).
  </read_first>
  <action>
    From the project root, install jest-axe as a dev dependency only: run `npm install -D jest-axe`.
    jest-axe is the RESEARCH-approved package (OK verdict, 2.2M downloads/wk, github.com/nickcolley/jest-axe,
    verified live on the npm registry during research) — it is NOT a flagged package, so no human checkpoint
    is required for this install. It is test-only and never ships in the production bundle.
    Do NOT install `lighthouse` in this plan: lighthouse carries a SUS ("too-new") recency flag and its
    install is gated behind a human checkpoint in plan 10-12 — out of scope here.
    Do NOT install `@vercel/analytics` or `@vercel/speed-insights` here — those land in plan 10-09.
    Confirm the postinstall `prisma generate` step (already in package.json scripts) completes without error.
  </action>
  <verify>
    <automated>node -e "require('jest-axe'); console.log('jest-axe resolvable')"</automated>
    <automated>node -e "process.exit(require('./package.json').devDependencies['jest-axe'] ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `jest-axe` appears under `devDependencies` in package.json (and package-lock.json is updated).
    - `node -e "require('jest-axe')"` exits 0 (importable under Node/Vitest).
    - No `lighthouse`, `@vercel/analytics`, or `@vercel/speed-insights` added by this plan.
  </acceptance_criteria>
  <done>jest-axe is installed dev-only and importable; no other packages added.</done>
</task>

<task type="auto">
  <name>Task 2: Author the four pure/mockable logic RED scaffolds</name>
  <files>tests/reviews.test.ts, tests/size-guides.test.ts, tests/seo.test.ts, tests/analytics.test.ts</files>
  <read_first>
    - `tests/orders.test.ts` and `tests/catalog.test.ts` — copy their import style (`import { describe, expect, it } from "vitest"`), the `@/lib/...` alias, and the describe/it structure exactly.
    - `lib/orders.ts` — `parseOrderItems` and the `OrderItem` shape (`slug`, `name`, `size`, `unitPrice`, `qty`) the reviews eligibility contract reuses; `lib/db.ts` — the `prisma` named export the reviews test mocks.
    - `.planning/phases/10-trust-polish/10-RESEARCH.md` Patterns 2, 3, 4, 5 (verified-purchase scan, bounded rating aggregation, JSON-LD builder + `<`-escape, consent-gated trackEvent) and §"Security Domain" (rating allow-list, JSON-LD escape, no-PII payloads).
    - `.planning/phases/10-trust-polish/10-VALIDATION.md` Per-Task Verification Map rows for TRST-01, TRST-02, SEO-01, ANLY-01.
  </read_first>
  <action>
    Create four Vitest files. Every file is RED-by-design: it statically imports from a `@/lib/*` module
    that does not exist yet, so the file fails to load until the corresponding later plan creates that module.
    Do NOT create any `lib/*` module in this plan. Write real, precise assertions (not placeholders) so each
    file becomes a true GREEN target once its module lands. Name describe blocks so the `-t` filters in
    10-VALIDATION.md resolve.

    File `tests/reviews.test.ts` — targets `lib/reviews.ts` (created in 10-03; Prisma-importing). At the top,
    hoist a `vi.mock("@/lib/db", ...)` whose factory returns an object with a `prisma` property exposing
    `order.findMany` and `review.groupBy` as `vi.fn()`s; import the mocked `prisma` from `@/lib/db`; import
    `isValidRating`, `hasPurchased`, and `getRatingSummaries` from `@/lib/reviews`; add a `beforeEach` that
    clears mocks. Three describe blocks:
      - "isValidRating" — assert it returns true for each of 1, 2, 3, 4, 5 and false for 0, 6, -1, 2.5, and NaN
        (integer 1 to 5 allow-list; mirrors the isValidOrderStatus idiom in lib/orders.ts).
      - "hasPurchased (verified-purchase slug match)" — set `prisma.order.findMany` to resolve to an array of
        orders whose `items` field is a JSON string (build it with JSON.stringify over line items each carrying
        a `slug`). Assert `await hasPurchased("user-1", "jacket-01")` is true when a resolved order's parsed
        items contain that slug, false when none contain it, and false when findMany resolves to an empty array.
      - "getRatingSummaries rating-merge zero-default" — set `prisma.review.groupBy` to resolve to a single
        entry for productId "p1" with `_avg.rating` 4.5 and `_count.rating` 2. Assert `getRatingSummaries(["p1","p2"])`
        resolves to a Map where `.get("p1")` deep-equals `{ avg: 4.5, count: 2 }` and `.get("p2")` deep-equals
        `{ avg: 0, count: 0 }` (the contract: the returned Map covers EVERY requested id, defaulting ids with no
        reviews to zero). Assert `getRatingSummaries([])` resolves to an empty Map.

    File `tests/size-guides.test.ts` — targets `lib/size-guides.ts` (created in 10-07; Prisma-free). Import
    `SIZE_GUIDE_CATEGORIES` and `getSizeGuide` from `@/lib/size-guides`. Assertions:
      - `SIZE_GUIDE_CATEGORIES` deep-equals `["Outerwear", "Knitwear", "Tees", "Accessories"]` (D-05 order).
      - For each of those four categories, `getSizeGuide(category)` is defined, its `category` equals the input,
        its `caption` is a non-empty string, its `columns` array is non-empty, its `rows` array is non-empty, and
        every row exposes a non-empty `size` string plus a `cells` array whose length equals `columns.length`
        (size guide table shape: measurement-column headers in `columns`, one `{ size, cells }` per size row).
      - `getSizeGuide("Hats")` and `getSizeGuide("")` both return undefined (sane fallback for unknown category).

    File `tests/seo.test.ts` — targets `lib/seo.ts` (created in 10-11; Prisma-free). Import `buildProductJsonLd`
    and `serializeJsonLd` from `@/lib/seo`. Use a sample product object shaped as `{ name, images (string array),
    description, price (integer cents), inStock (boolean), rating: { avg, count } }`. Assertions:
      - Shape: `buildProductJsonLd(product)` returns an object whose `@context` is "https://schema.org", `@type`
        is "Product", `name` equals the product name, `image` equals the images array, `description` equals the
        product description, and `offers` is an object with `@type` "Offer", `priceCurrency` "USD", `price` equal
        to the price-in-cents divided by 100 formatted to two decimals, and `availability` equal to the schema.org
        InStock URL when inStock is true and the OutOfStock URL when false.
      - aggregateRating conditional (D-04): when `rating.count` is greater than 0, the returned object has an
        `aggregateRating` sub-object with `@type` "AggregateRating", `ratingValue` equal to avg formatted to one
        decimal, and `reviewCount` equal to the count; when `rating.count` is 0 the returned object does NOT have
        the aggregateRating property (assert with `not.toHaveProperty("aggregateRating")`).
      - Escaping: `serializeJsonLd` takes a JSON-LD object and returns a string. Given an object whose description
        contains a script-closing sequence and an angle bracket, assert the returned string contains no raw
        less-than character and does contain its unicode escape (the `<` form) — this pins the Next.js
        official inline-JSON-LD XSS mitigation.

    File `tests/analytics.test.ts` — targets `lib/analytics.ts` (created in 10-09; browser/localStorage). At the
    top, hoist a `vi.mock("@vercel/analytics", ...)` whose factory returns an object with `track` as a `vi.fn()`;
    import the mocked `track` from `@vercel/analytics`; import `getConsent`, `setConsent`, `hasConsent`, and
    `trackEvent` from `@/lib/analytics`; add a `beforeEach` that calls `localStorage.clear()` and clears mocks.
    Two describe blocks:
      - "consent persistence" — assert `getConsent()` is null before any choice; after `setConsent("accepted")`,
        `getConsent()` is "accepted" and `hasConsent()` is true; after `setConsent("declined")`, `hasConsent()`
        is false.
      - "trackEvent consent gating" — with no stored consent, `trackEvent("view_product", { slug: "x" })` must
        NOT call the mocked `track`; after `setConsent("declined")`, `trackEvent(...)` must NOT call `track`;
        after `setConsent("accepted")`, `trackEvent("add_to_cart", { slug: "x" })` calls `track` exactly once
        with arguments "add_to_cart" and `{ slug: "x" }`. Add a case where the mocked `track` is set to throw and
        assert `trackEvent` (after accept) does not throw (analytics must never break the app).

    Keep all four files free of `@/lib/*` implementation — only the tests. Each file must be valid TypeScript/TSX
    that Vitest can COLLECT; it fails only because its target `@/lib/*` module is missing.
  </action>
  <verify>
    <automated>npx vitest run tests/orders.test.ts tests/catalog.test.ts tests/admin.test.ts tests/wishlist.test.ts tests/smoke.test.ts</automated>
    <automated>! npx vitest run tests/reviews.test.ts tests/size-guides.test.ts tests/seo.test.ts tests/analytics.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The four files exist at `tests/reviews.test.ts`, `tests/size-guides.test.ts`, `tests/seo.test.ts`, `tests/analytics.test.ts`.
    - Running the four new files fails (RED) — the failure is an unresolved `@/lib/{reviews,size-guides,seo,analytics}` import (the module does not exist yet), NOT a TypeScript syntax error inside the test file.
    - The pre-existing suite (orders, catalog, admin, wishlist, smoke) still passes with exit 0 — this plan added nothing that regresses it.
    - No `lib/reviews.ts`, `lib/size-guides.ts`, `lib/seo.ts`, or `lib/analytics.ts` was created by this plan.
    - Describe/test names contain the tokens `isValidRating`, `hasPurchased`, `getRatingSummaries` (reviews) so 10-VALIDATION.md `-t` filters resolve.
  </acceptance_criteria>
  <done>Four RED logic scaffolds exist and fail only on missing `@/lib/*` targets; the existing suite is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Author the jest-axe component-a11y RED scaffold</name>
  <files>tests/a11y.test.tsx</files>
  <read_first>
    - `.planning/phases/10-trust-polish/10-UI-SPEC.md` §1 (ReviewForm states + radiogroup star input), §3 (SizeGuideModal dialog a11y contract), §4 (ConsentBanner) — the components this file will render.
    - `components/ProductCard.tsx` / `components/AddToCart.tsx` — confirm storefront components use `export default` (the dynamic imports below read `.default`).
    - `vitest.config.ts` — jsdom + `@vitejs/plugin-react` already configured so `.tsx` render + jest-axe work.
    - `.planning/phases/10-trust-polish/10-VALIDATION.md` PERF-01 rows and the `-t ReviewForm` / `-t SizeGuideModal` / `-t ConsentBanner` filter commands.
  </read_first>
  <action>
    Create `tests/a11y.test.tsx`. At the top import `render` from `@testing-library/react`, import `axe` and
    `toHaveNoViolations` from `jest-axe` (installed in Task 1), and call `expect.extend(toHaveNoViolations)`.
    Add an `afterEach` that cleans up the DOM (`cleanup` from @testing-library/react).

    Author three independent describe blocks named exactly "ReviewForm a11y", "SizeGuideModal a11y", and
    "ConsentBanner a11y" so the `-t ReviewForm` / `-t SizeGuideModal` / `-t ConsentBanner` filters resolve and
    each block can be turned green independently by its owning plan (10-05, 10-07, 10-09). CRITICAL: import each
    component with a DYNAMIC import INSIDE its test (read the module's `.default` export, matching the storefront's
    default-export convention) — never a static top-level import. This keeps the file loadable even while the
    other components are still missing, so a passing block does not depend on siblings existing. Each test is async:
      - "ReviewForm a11y" — dynamically import `@/components/ReviewForm`, render it in its default eligible state
        with minimal props implied by UI-SPEC §1 (e.g. a `productSlug`), then assert `await axe(container)` has
        no violations. Add a short in-file comment noting that plan 10-05 owns finalizing the exact props and any
        session/provider wrapper needed to render it; the fixed contract is the zero-violations axe assertion.
      - "SizeGuideModal a11y" — dynamically import `@/components/SizeGuideModal`, render it OPEN with a sample
        size-guide object and an `onClose` handler (per UI-SPEC §3 dialog contract), then assert `await axe(container)`
        has no violations. Comment that plan 10-07 finalizes props.
      - "ConsentBanner a11y" — dynamically import `@/components/ConsentBanner`, render it in its visible state,
        then assert `await axe(container)` has no violations. Comment that plan 10-09 finalizes props.

    Do NOT create any component in this plan. In Wave 0 each block fails because its dynamic import rejects
    (the component file does not exist) — that is the intended RED. Because the imports are dynamic and per-block,
    the failures are independent, exactly as later plans require.
  </action>
  <verify>
    <automated>node -e "require('jest-axe'); require('@testing-library/react'); console.log('a11y deps resolvable')"</automated>
    <automated>! npx vitest run tests/a11y.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `tests/a11y.test.tsx` exists and Vitest can COLLECT it (top-level imports — testing-library, jest-axe — all resolve; `expect.extend(toHaveNoViolations)` is wired).
    - The file contains exactly three describe blocks named "ReviewForm a11y", "SizeGuideModal a11y", "ConsentBanner a11y", each using a dynamic per-test import of its component's default export.
    - Running the file fails RED because the three `@/components/*` targets do not exist yet — not because of a jest-axe/setup error.
    - No component file (`ReviewForm`, `SizeGuideModal`, `ConsentBanner`) was created by this plan.
  </acceptance_criteria>
  <done>The a11y scaffold loads, wires jest-axe, and fails RED per-block on missing components; each block can green independently later.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**Files created**
- `tests/reviews.test.ts`, `tests/size-guides.test.ts`, `tests/seo.test.ts`, `tests/analytics.test.ts`, `tests/a11y.test.tsx`
- `jest-axe` added to `package.json` devDependencies (+ package-lock.json)

**Contracts pinned (the exact API each later plan MUST implement to turn a scaffold green)**

`lib/reviews.ts` (satisfied by 10-03):
- `isValidRating(value: number): boolean` — true only for integers 1–5.
- `hasPurchased(userId: string, productSlug: string): Promise<boolean>` — reads the user's own eligible orders via prisma, scans parsed `Order.items` (reuse `parseOrderItems`) for a slug match; matches by **slug**, never productId (RESEARCH Pitfall 1).
- `type RatingSummary = { avg: number; count: number }`.
- `getRatingSummaries(productIds: string[]): Promise<Map<string, RatingSummary>>` — returned Map covers **every** requested id, defaulting ids with no reviews to `{ avg: 0, count: 0 }` (RESEARCH Pitfall 2); empty input → empty Map; uses `prisma.review.groupBy` (no N+1).

`lib/size-guides.ts` (satisfied by 10-07):
- `SIZE_GUIDE_CATEGORIES = ["Outerwear","Knitwear","Tees","Accessories"]`.
- `type SizeGuide = { category: string; caption: string; columns: string[]; rows: Array<{ size: string; cells: string[] }> }` (measurement-column headers in `columns`; one row per size; `cells.length === columns.length`).
- `getSizeGuide(category: string): SizeGuide | undefined` — defined for the four categories, undefined otherwise.

`lib/seo.ts` (satisfied by 10-11):
- `buildProductJsonLd(product): object` — schema.org Product + Offer; includes `aggregateRating` only when `rating.count > 0` (D-04).
- `serializeJsonLd(jsonLd: object): string` — `JSON.stringify` with the `<` → `<` escape (RESEARCH Pattern 4 / Pitfall 5).

`lib/analytics.ts` (satisfied by 10-09):
- `getConsent(): "accepted" | "declined" | null`, `setConsent(value): void`, `hasConsent(): boolean`.
- `trackEvent(name, payload?): void` — no-op unless `hasConsent()`; calls `@vercel/analytics` `track` only when accepted; swallows errors; payloads carry only non-PII fields (RESEARCH Security Domain).

Components (default exports; satisfied by 10-05 / 10-07 / 10-09):
- `components/ReviewForm.tsx`, `components/SizeGuideModal.tsx`, `components/ConsentBanner.tsx` — each must render with zero axe violations.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → npm registry | `npm install -D jest-axe` pulls a third-party package (supply-chain surface) |

This plan introduces **no runtime trust boundaries**: it writes test files and installs one
dev-only dependency. No user input, no network calls, no DB writes, no production-bundle code.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-SC | Tampering | `npm install -D jest-axe` | medium | mitigate | jest-axe verified in 10-RESEARCH §Package Legitimacy Audit (OK / Approved: 2.2M dl/wk, github.com/nickcolley/jest-axe, live on npm). Dev/test-only — never in the production bundle. No `[ASSUMED]`/`[SUS]` package installed here, so no human checkpoint required. The SUS `lighthouse` install is explicitly deferred to 10-12's blocking-human checkpoint. |
| T-10-01 | Information Disclosure | analytics RED scaffold | low | accept | `tests/analytics.test.ts` pins the no-PII payload contract (slug-only), reinforcing the RESEARCH Security-Domain guard before any call site exists — no runtime exposure in this plan. |
</threat_model>

<verification>
## Overall phase checks (this plan)

- `node -e "require('jest-axe')"` → exits 0 (Task 1).
- `npx vitest run tests/orders.test.ts tests/catalog.test.ts tests/admin.test.ts tests/wishlist.test.ts tests/smoke.test.ts` → exits 0 (existing suite unaffected).
- `npx vitest run tests/reviews.test.ts tests/size-guides.test.ts tests/seo.test.ts tests/analytics.test.ts tests/a11y.test.tsx` → **fails (RED) by design** — every new file fails only on a missing `@/lib/*` or `@/components/*` target it pins, not on a scaffold syntax/setup error. Run via the Bash tool; the `!`-negated commands in each task assert this RED state succeeds.

**Do not attempt to make the five new files green in this plan.** RED is the deliverable.
Green is owned by 10-03 (reviews), 10-07 (size-guides), 10-11 (seo), 10-09 (analytics),
and 10-05/10-07/10-09 (a11y component blocks).
</verification>

<success_criteria>
- `jest-axe` present in package.json devDependencies and importable.
- Five test files exist: `tests/reviews.test.ts`, `tests/size-guides.test.ts`, `tests/seo.test.ts`, `tests/analytics.test.ts`, `tests/a11y.test.tsx`.
- Each new file is collectible by Vitest and fails RED strictly because its `@/lib/*` / `@/components/*` target is not yet implemented.
- The pre-existing 5-file suite still passes; no `lib/*` module or storefront component was created by this plan.
- Describe/test names expose the `-t` tokens (`isValidRating`, `hasPurchased`, `getRatingSummaries`, `ReviewForm`, `SizeGuideModal`, `ConsentBanner`) used by 10-VALIDATION.md.
</success_criteria>

<output>
Create `.planning/phases/10-trust-polish/10-01-SUMMARY.md` when done.
</output>
