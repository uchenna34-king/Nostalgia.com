# Decisions (from ADRs)

_No ADR-classified documents were ingested. This file is intentionally empty._

Architectural decisions are implied by the SPEC and were realized in the delivered
v1 slice. They are captured as constraints (see `constraints.md`) rather than as
formal, locked ADR decisions. Downstream consumers should treat none of the
following as LOCKED — they may be revisited without a superseding-ADR gate:

- Next.js 14 App Router + TypeScript + Tailwind (SPEC-derived, not locked)
- NextAuth with Google provider + dev-mock fallback (SPEC-derived, not locked)
- Prisma + SQLite for local persistence (SPEC-derived, not locked)
- Stripe Checkout test mode with stub fallback (SPEC-derived, not locked)
- Cart via React context + localStorage (SPEC-derived, not locked)

source: (none — no ADRs in ingest set)
