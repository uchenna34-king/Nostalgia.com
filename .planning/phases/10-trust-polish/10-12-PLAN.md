---
phase: 10-trust-polish
plan: 12
type: execute
wave: 6
depends_on: ["10-01", "10-02", "10-03", "10-04", "10-05", "10-06", "10-07", "10-08", "10-09", "10-10", "10-11"]
files_modified:
  - next.config.js
  - app/globals.css
  - components/Hero.tsx
  - components/ProductCard.tsx
  - components/Gallery.tsx
  - app/page.tsx
autonomous: false
requirements: [PERF-01]

must_haves:
  truths:
    - "The `<img>` tags on the home page (`app/page.tsx`, `components/Hero.tsx`), the shop/PDP product grids (`components/ProductCard.tsx`), and the PDP gallery (`components/Gallery.tsx`) are migrated to `next/image` with explicit dimensions (via `fill` over sized containers), a `sizes` hint, `priority` on the above-the-fold LCP images, and default lazy loading below the fold — no raw `<img>`/`eslint-disable @next/next/no-img-element` remains on those surfaces (D-15). [PERF-01]"
    - "A global `:focus-visible` keyboard-focus ring (sepia, per the UI-SPEC focus-ring color) is applied in `app/globals.css` so every interactive control shows a visible focus indicator (WCAG 2.4.7), and existing alt text + reduced-motion handling are preserved through the migration (D-15). [PERF-01]"
    - "The full `tests/a11y.test.tsx` jest-axe suite is green (ReviewForm, SizeGuideModal, ConsentBanner, RatingStars and any other Phase 10 component report zero structural violations), and the full `npx vitest run` suite plus `npx tsc --noEmit` pass with no regression (D-15). [PERF-01]"
    - "A human has signed off on the `lighthouse` package legitimacy (flagged SUS on publish recency) BEFORE any `lighthouse` invocation, per the package-legitimacy protocol (D-15). [PERF-01]"
    - "Lighthouse runs against a PRODUCTION build (`npm run build` then `npm run start -- -p 3002`, per RESEARCH Pitfall 6) score performance >=90 and accessibility 100 on home (`/`), `/shop`, and a real PDP; the numeric scores are recorded in `10-12-SUMMARY.md` (D-15). [PERF-01]"
  prohibitions:
    - "No Lighthouse performance measurement against the dev server (`next dev`) — dev builds are unminified/unoptimized and produce misleading scores; performance MUST be measured against `npm run build && npm run start` (RESEARCH Pitfall 6)."
    - "No `npx lighthouse` / `npm install lighthouse` invocation before the blocking-human legitimacy checkpoint (Task 3) is approved — the package is flagged SUS and the checkpoint is never auto-approvable."
    - "No removal or weakening of existing accessibility semantics during the image migration: alt text (`product.name`, gallery view labels), `aria-hidden` on decorative hover/thumbnail images, and `motion-reduce:` transitions must be preserved or strengthened, never dropped."
  artifacts:
    - next.config.js
    - app/globals.css
    - components/Hero.tsx
    - components/ProductCard.tsx
    - components/Gallery.tsx
    - app/page.tsx
    - .planning/phases/10-trust-polish/10-12-SUMMARY.md
  key_links:
    - "`next.config.js` `images.dangerouslyAllowSVG` + sandbox CSP <-> `next/image` rendering the first-party `/products/*.svg` placeholder assets without an optimizer 400 <-> the images actually painting when running `npm run start`"
    - "`app/globals.css` `:focus-visible` sepia ring <-> every keyboard-focusable control across the storefront (Nav links/buttons, product-card links, gallery thumbnails, add-to-cart)"
    - "Production build (`npm run build && npm run start -- -p 3002`) <-> `npx lighthouse http://localhost:3002/<route> --only-categories=performance,accessibility` for `/`, `/shop`, `/product/sepia-wool-overcoat` <-> the perf>=90 / a11y 100 scores documented in the SUMMARY"
    - "The `lighthouse` SUS-package blocking-human checkpoint (Task 3) <-> any `lighthouse` invocation in Task 4 (checkpoint gates the run)"
---

<objective>
Final cross-cutting Trust & Polish pass (PERF-01, D-15): migrate the storefront's image surfaces to `next/image`, remediate accessibility across home / `/shop` / PDP (semantic landmarks already present, plus a global keyboard focus-visible ring, preserved alt text, palette-safe contrast, reduced-motion), drive the full `tests/a11y.test.tsx` suite green, then — after a human signs off on the SUS-flagged `lighthouse` package — measure Lighthouse performance and accessibility against a PRODUCTION build on all three routes and document the scores.

Purpose: This is the last hardening plan before go-live (Phase 11). Everything shipped in Phases 8-10 (home, `/shop`, PDP, reviews, size guide, consent banner) is audited here so the store meets the locked D-15 targets — WCAG 2.1 AA and Lighthouse >=90 performance / 100 accessibility — measured correctly (production build, not dev; RESEARCH Pitfall 6) and behind the mandated package-legitimacy checkpoint.

Output: `next/image` migration + `next.config.js` image config, a global `:focus-visible` ring and reduced-motion hardening in `app/globals.css`, a green full test + a11y suite, a blocking-human `lighthouse` legitimacy sign-off, and per-route Lighthouse scores recorded in `10-12-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-trust-polish/10-CONTEXT.md
@.planning/phases/10-trust-polish/10-RESEARCH.md
@.planning/phases/10-trust-polish/10-UI-SPEC.md
@.planning/phases/10-trust-polish/10-VALIDATION.md
@next.config.js
@app/globals.css
@app/layout.tsx
@components/AppFrame.tsx
@components/Hero.tsx
@components/ProductCard.tsx
@components/Gallery.tsx
@app/page.tsx
@app/shop/page.tsx
@app/product/[slug]/page.tsx
</context>

## Artifacts this plan produces

- `next.config.js` — extended with an `images` config (`dangerouslyAllowSVG: true` + `contentDispositionType: "attachment"` + a sandbox `contentSecurityPolicy`) so the first-party `/products/*.svg` placeholder assets render through the `next/image` optimizer without a 400. Harmless once real raster photography replaces the placeholders in a later phase.
- `app/globals.css` — a global `:focus-visible` keyboard focus ring (2px sepia outline + offset) in `@layer base`, plus any reduced-motion hardening the audit requires.
- `components/Hero.tsx` — the two above-the-fold collage `<img>`s migrated to `next/image` (`fill` + `priority` LCP, `sizes`), alt text preserved.
- `components/ProductCard.tsx` — the primary + decorative-hover product-grid `<img>`s migrated to `next/image` (`fill` + `sizes`, default lazy), `alt={product.name}` and the `aria-hidden` hover image preserved.
- `components/Gallery.tsx` — the main PDP image (`fill` + `priority`) and thumbnail `<img>`s migrated to `next/image`, existing thumbnail a11y (`aria-pressed`/`aria-current`/labels) and `motion-reduce` preserved.
- `app/page.tsx` — the editorial-band `<img>` migrated to `next/image` (`fill`, lazy), alt preserved.
- `.planning/phases/10-trust-polish/10-12-SUMMARY.md` — records the per-route Lighthouse performance + accessibility scores.

This plan is the Wave 6 final pass and depends on every prior Phase 10 plan (10-01 through 10-11): the Phase 10 components it audits (ReviewForm, SizeGuideModal, ConsentBanner, RatingStars) and the `tests/a11y.test.tsx` suite must already exist. It is `autonomous: false` because Task 3 is a blocking-human package-legitimacy checkpoint.

<tasks>

<task type="auto">
  <name>Task 1: Migrate storefront images to next/image + global focus-visible + reduced-motion remediation</name>
  <files>next.config.js, app/globals.css, components/Hero.tsx, components/ProductCard.tsx, components/Gallery.tsx, app/page.tsx</files>
  <read_first>
    - components/Hero.tsx — the two collage `<img>`s (lines 30-42) inside `col-span-1 ... aspect-[3/4] w-full object-cover` containers, each with real alt ("Sepia Wool Overcoat", "Nostalgia Hoodie"); these are above the fold (LCP candidates).
    - components/ProductCard.tsx — the primary `<img>` (`alt={product.name}`) and the decorative hover `<img>` (`alt="" aria-hidden`) inside the `relative aspect-[3/4] overflow-hidden bg-cream-dark` container (lines 9-24); the `group-hover:scale-[1.04]` transform and hover opacity transition. Note this file was already extended in 10-04 with a compact rating row below price — preserve that row untouched.
    - components/Gallery.tsx — the `"use client"` main image (`alt={`${name} view N`}`) in the `aspect-[3/4] overflow-hidden bg-cream-dark` container (NOT currently `relative`) and the thumbnail `<img>`s (`alt="" aria-hidden`) inside `aspect-[3/4]` buttons; the existing `motion-reduce:transition-none` and `aria-pressed`/`aria-current`/`aria-label` semantics.
    - app/page.tsx — the editorial-band `<img src="/products/archive-bomber-jacket-1.svg" alt="Archive Bomber Jacket">` (lines 58-63) in an `aspect-[4/5] w-full object-cover` container (below the fold).
    - next.config.js — currently only `{ reactStrictMode: true }`; the `images` key must be added.
    - app/globals.css — the `@layer base` block (lines 9-26) and the existing `@media (prefers-reduced-motion: reduce)` block (lines 61-69); the `sepia` token is `#A6552F` (see tailwind.config.ts). There is currently NO `:focus-visible` styling.
    - .planning/phases/10-trust-polish/10-RESEARCH.md — Pitfall 6 (measure perf against a production build) and the D-15 checklist (next/image, semantic landmarks, focus states, alt text, contrast, reduced-motion). .planning/phases/10-trust-polish/10-UI-SPEC.md — the "focus-ring color" = sepia and the reduced-motion contract.
  </read_first>
  <action>
    Extend `next.config.js`: add an `images` object to the exported config with `dangerouslyAllowSVG: true`, `contentDispositionType: "attachment"`, and `contentSecurityPolicy: "default-src 'self'; script-src 'none'; sandbox;"`. Add a one-line comment stating this is required only so the first-party committed `/products/*.svg` placeholder assets pass through the `next/image` optimizer; it is safe because these SVGs are our own committed placeholders (not user uploads) and becomes a no-op once real raster photography replaces them. If the executor prefers, an equivalent alternative is to set `unoptimized` on each SVG-backed `<Image>` instead of the config flag — pick one approach consistently; the config flag is preferred because it keeps the JSX raster-ready.

    Migrate the images on all four surfaces to `next/image` (`import Image from "next/image"`). Use the `fill` layout over the existing aspect-ratio containers (the parent must be `position: relative` — add `relative` where the container lacks it, e.g. Gallery's main-image and thumbnail wrappers), keep `className="object-cover ..."` on the `Image`, and provide a `sizes` hint appropriate to each container's rendered width. Preserve every existing `alt` value verbatim, and keep decorative images (`ProductCard` hover image, `Gallery` thumbnails) as `alt=""` with `aria-hidden`. Set `priority` ONLY on the above-the-fold LCP images: both `Hero` collage images and the `Gallery` main image. Leave everything else lazy (the `next/image` default — do not set `priority` on `ProductCard` grid images or the `app/page.tsx` editorial image). Remove the now-unnecessary `eslint-disable-next-line @next/next/no-img-element` comments on the migrated tags. On `ProductCard`, keep the `group-hover:scale-[1.04]` transform but add `motion-reduce:transition-none` so the hover motion collapses under `prefers-reduced-motion` (D-15 reduced-motion).

    In `app/globals.css`, inside `@layer base`, add a global `:focus-visible` rule giving a visible keyboard focus indicator: a 2px solid sepia (`#A6552F`, or `theme(colors.sepia)`) outline with `outline-offset: 2px`. This applies the WCAG 2.4.7 focus indicator uniformly across Nav links/buttons, product-card links, gallery thumbnail buttons, add-to-cart controls, and form fields. Do NOT suppress default outlines anywhere. Do NOT add a skip-link this pass — the `<main>` (each page), `<header>`/`<nav>` (Nav), and `<footer>` (Footer) landmarks already exist, and Lighthouse's automated a11y audits do not require a bypass link; leaving it out keeps this pass from touching every page file.

    Do not change any layout dimensions, aspect ratios, palette colors, or copy — this is a mechanical image/optimization + focus-ring pass only, preserving the locked aesthetic.
  </action>
  <verify>
    <automated>npm run build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 and `npx tsc --noEmit` exits 0.
    - `components/Hero.tsx`, `components/ProductCard.tsx`, `components/Gallery.tsx`, and `app/page.tsx` each import `next/image` and use `<Image>` in place of the prior `<img>`; no `eslint-disable ... no-img-element` line remains on those migrated tags (`npm run lint` reports no `@next/next/no-img-element` warnings for these files).
    - `Hero`'s two collage images and `Gallery`'s main image set `priority`; `ProductCard` grid images and the `app/page.tsx` editorial image do not.
    - Existing alt text is preserved: `ProductCard` primary image still uses `alt={product.name}`, its hover image stays `alt=""`/`aria-hidden`, and `Gallery` main image keeps its `${name} view N` label.
    - `next.config.js` includes an `images` config with `dangerouslyAllowSVG: true` and a sandbox `contentSecurityPolicy` (or every SVG-backed `<Image>` sets `unoptimized`).
    - `app/globals.css` contains a `:focus-visible` rule with a sepia outline; the `ProductCard` hover transform carries `motion-reduce:transition-none`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Drive the full a11y suite + full test suite green, remediating any component violations</name>
  <files>tests/a11y.test.tsx (read-only run; edit only Phase 10 component files if violations surface)</files>
  <read_first>
    - tests/a11y.test.tsx — the jest-axe suite created in 10-01 and made green incrementally by 10-05 (ReviewForm), 10-07 (SizeGuideModal), and 10-09 (ConsentBanner); confirm which components it renders and asserts zero violations on.
    - .planning/phases/10-trust-polish/10-VALIDATION.md — the "Per-Task Verification Map" PERF-01 rows (component jest-axe, `npx vitest run tests/a11y.test.tsx`) and the full-suite / `tsc` sampling contract.
    - The Phase 10 component files the suite covers (e.g. `components/ReviewForm.tsx`, `components/SizeGuideModal.tsx`, `components/ConsentBanner.tsx`, `components/RatingStars.tsx`) — only if a violation surfaces and needs a targeted fix (missing label, role, aria pairing, contrast).
  </read_first>
  <action>
    Run the full component-a11y suite: `npx vitest run tests/a11y.test.tsx`. It should already be green by Wave 6. If any Phase 10 component reports a structural axe violation (missing accessible name, missing/incorrect `role`, unlabeled control, an aria-required-children/parent break, or a color-contrast failure within the locked palette), fix it at the component source — the smallest change that satisfies axe without altering the locked visual design (add an `aria-label`/`aria-labelledby`, correct a role, pair a glyph with a text/aria equivalent, or nudge a token to the AA-passing sibling already in the palette). Do NOT weaken the tests to make them pass; fix the component.

    Then run the full suite `npx vitest run` and `npx tsc --noEmit` to confirm no regression from Task 1's image migration or any component fix here. If the full suite surfaces a failure caused by the `next/image` migration (e.g. a component test that asserted on a raw `<img>`), update that test to the new `next/image` output only where the assertion is about the migrated markup — not to hide a real a11y regression.
  </action>
  <verify>
    <automated>npx vitest run tests/a11y.test.tsx && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/a11y.test.tsx` exits 0 (zero axe violations across all Phase 10 components it renders).
    - `npx vitest run` (full suite) exits 0 — no regression in the pre-existing suites (`catalog`, `orders`, `admin`, `wishlist`, `smoke`, `reviews`, `size-guides`, `seo`, `analytics`).
    - `npx tsc --noEmit` exits 0.
    - Any fix made was to a component source (or a migrated-markup assertion), never a loosened a11y assertion.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Blocking-human sign-off on lighthouse package legitimacy (SUS)</name>
  <action>
    HALT autonomous execution here and obtain explicit human approval before any `lighthouse` package fetch, install, or `npx` invocation. This is a blocking-human, never-auto-approvable package-legitimacy gate (the package is flagged SUS on publish recency). Present the `<what-built>` context and the `<how-to-verify>` npmjs.com provenance steps to the human, wait for the `<resume-signal>`, and record the approval outcome (approved via npx / approved via install / halted) in the plan run notes. Do NOT begin Task 4 until this checkpoint is approved.
  </action>
  <what-built>
    Phase 10 research flagged the `lighthouse` npm package as SUS purely on publish recency (a routine release ~3 days before the research date). Its identity signals are all strong — it is the official `github.com/GoogleChrome/lighthouse` project, ~3.6M downloads/week — but the package-legitimacy protocol requires an explicit human sign-off before running or installing it. Task 4 (the production-build Lighthouse measurement) MUST NOT run until this is approved.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/lighthouse and confirm the package is published by the Google Chrome / Lighthouse maintainers and links to `github.com/GoogleChrome/lighthouse` (the official repo), with weekly downloads in the millions.
    2. Confirm the recency flag is a routine version bump (normal release cadence), not a hijack/typosquat — the package name is exactly `lighthouse` (no lookalike characters), and the repo/owner match the official Google Chrome project.
    3. Decide how Task 4 obtains it: either approve running it via `npx lighthouse` (no persistent install, pulls the official package on demand) or `npm install -D lighthouse`. Either is acceptable once you approve; `npx` is preferred (no committed dependency).
  </how-to-verify>
  <resume-signal>Type "approved" (optionally "approved via npx" or "approved via install") to unblock Task 4, or describe a concern to halt the Lighthouse run.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Production-build Lighthouse runs on home / shop / PDP + document scores</name>
  <files>.planning/phases/10-trust-polish/10-12-SUMMARY.md</files>
  <read_first>
    - .planning/phases/10-trust-polish/10-RESEARCH.md — Pitfall 6 (measure performance against `npm run build && npm run start`, never `next dev`) and the D-15 targets (perf >=90 / a11y 100 on home, `/shop`, PDP). The Environment Availability row confirming Chrome at `C:\Program Files\Google\Chrome\Application\chrome.exe`.
    - .planning/phases/10-trust-polish/10-VALIDATION.md — the manual Lighthouse verification row and the exact invocation shape (`--only-categories=performance,accessibility --output=json --chrome-flags="--headless=new"`).
    - prisma/seed.ts — confirm a real product slug exists for the PDP route (e.g. `sepia-wool-overcoat`, `archive-bomber-jacket`); pick any seeded slug that renders.
  </read_first>
  <action>
    ONLY after Task 3 is approved. Build and serve a PRODUCTION build (Pitfall 6): run `npm run build`, then start the production server on port 3002 with `npm run start -- -p 3002` (run it in the background so Lighthouse can hit it). Confirm the server is up (e.g. the home page responds on `http://localhost:3002/`), and load `/`, `/shop`, and the PDP once in a browser/`curl` to confirm the migrated `next/image` SVG placeholders actually render (no optimizer 400s / broken images) before scoring.

    Run Lighthouse via the approved method (npx or installed) against the production server for all three routes, capturing JSON: home `http://localhost:3002/`, shop `http://localhost:3002/shop`, and a real PDP `http://localhost:3002/product/<seeded-slug>` (e.g. `/product/sepia-wool-overcoat`). Use `--only-categories=performance,accessibility --output=json --chrome-flags="--headless=new"` and write each to its own JSON file (e.g. `lighthouse-home.json`, `lighthouse-shop.json`, `lighthouse-pdp.json`). Read the `categories.performance.score` and `categories.accessibility.score` (0-1 scale) from each JSON.

    Record the six numbers (perf + a11y per route, as 0-100) in `10-12-SUMMARY.md` in a small results table with the route, perf score, a11y score, and pass/fail against the >=90 / 100 targets. If a target is missed, treat the specific Lighthouse audit findings (e.g. render-blocking, unsized image, contrast, missing label) as remediation items: fix the concrete cause on the relevant surface, rebuild, and re-run that route until it passes — do NOT lower the target. If a performance target genuinely cannot be reached in this local prod environment after remediation, document the residual gap and the specific audit(s) responsible rather than silently marking it passed. Do the lighthouse JSON files not need to be committed (they are measurement artifacts) — only the documented scores in the SUMMARY are the deliverable; add the `lighthouse-*.json` outputs to `.gitignore` if convenient. Stop the background production server when done.
  </action>
  <verify>
    <automated>npm run build && (npm run start -- -p 3002 &) && sleep 8 && npx lighthouse http://localhost:3002/ --only-categories=performance,accessibility --output=json --output-path=./lighthouse-home.json --chrome-flags="--headless=new" && npx lighthouse http://localhost:3002/shop --only-categories=performance,accessibility --output=json --output-path=./lighthouse-shop.json --chrome-flags="--headless=new" && npx lighthouse http://localhost:3002/product/sepia-wool-overcoat --only-categories=performance,accessibility --output=json --output-path=./lighthouse-pdp.json --chrome-flags="--headless=new" && node -e "const f=['home','shop','pdp'];let ok=true;for(const r of f){const j=require('./lighthouse-'+r+'.json');const p=j.categories.performance.score*100,a=j.categories.accessibility.score*100;console.log(r,'perf',p,'a11y',a);if(p<90||a<100)ok=false;}process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - Lighthouse ran against the production server (`npm run build` + `npm run start -- -p 3002`), NOT `next dev` (Pitfall 6).
    - All three routes (`/`, `/shop`, `/product/<seeded-slug>`) score performance >=90 and accessibility 100 (the `node -e` assertion exits 0), OR any residual gap is documented in the SUMMARY with the specific responsible audit(s).
    - `10-12-SUMMARY.md` contains a results table with per-route performance and accessibility scores (0-100) and their pass/fail against the >=90 / 100 targets.
    - The migrated `next/image` SVG placeholders render on all three routes (no optimizer 400s / broken images observed during the run).
    - Task 4 executed only after the Task 3 legitimacy checkpoint was approved.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev machine <- npm/npx package fetch (`lighthouse`) | A verification-only tool is pulled from the npm registry and executed locally against the production build. The package was flagged SUS (recency), so its provenance is the primary supply-chain surface of this plan. No new runtime/product dependency is added to the shipped app. |
| `next/image` optimizer <- first-party `/products/*.svg` assets | Serving SVGs through the optimizer normally requires `dangerouslyAllowSVG`. The assets are our own committed placeholders (not user uploads), and the sandbox CSP (`script-src 'none'; sandbox`) neutralizes any embedded script, keeping the SVG-through-optimizer path safe. |

## STRIDE Threat Register (ASVS L1 — block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-SC | Tampering (supply chain) | `lighthouse` npm/npx fetch | high | mitigate | Flagged SUS on publish recency only (official `GoogleChrome/lighthouse`, ~3.6M downloads/week, exact name — no typosquat signal). Gated behind the Task 3 `checkpoint:human-verify gate="blocking-human"` — a human confirms npmjs.com provenance before any `npx lighthouse`/`npm install lighthouse` runs. The checkpoint is never auto-approvable. `lighthouse` is a dev/verification tool only; it is not a runtime dependency of the shipped storefront. |
| T-10-IMG | Tampering | `next/image` + `images.dangerouslyAllowSVG` | low | accept | `dangerouslyAllowSVG` is enabled only for first-party committed placeholder SVGs, with a sandbox CSP (`default-src 'self'; script-src 'none'; sandbox;`) and `contentDispositionType: "attachment"` that prevent inline-script execution from a served SVG. No untrusted/user-supplied SVG source exists in M2 (real photography arrives later as raster). |
</threat_model>

<verification>
Run after all tasks complete, in order:

1. `npm run build` — production build succeeds with the migrated `next/image` surfaces and the `images` config.
2. `npx tsc --noEmit` — whole-repo typecheck passes.
3. `npx vitest run tests/a11y.test.tsx` — full component-a11y suite green (zero axe violations across Phase 10 components).
4. `npx vitest run` — full suite green (no regression in pre-existing suites).
5. Blocking-human checkpoint (Task 3): a human has signed off on the `lighthouse` package legitimacy.
6. Production-build Lighthouse (Task 4): `npm run start -- -p 3002` serving the production build, `npx lighthouse` on `/`, `/shop`, and `/product/<seeded-slug>` with `--only-categories=performance,accessibility` — performance >=90 and accessibility 100 on all three routes, scores recorded in `10-12-SUMMARY.md`.
</verification>

<success_criteria>
- Home, `/shop`, and PDP image surfaces are migrated to `next/image` (dimensions via `fill`, `sizes`, `priority` on LCP, lazy below fold) with alt text and reduced-motion preserved (D-15). [PERF-01]
- A global sepia `:focus-visible` ring gives every interactive control a visible keyboard focus indicator; existing semantic landmarks (`main`/`nav`/`header`/`footer`) remain intact (D-15). [PERF-01]
- The full `tests/a11y.test.tsx` suite is green, and `npx vitest run` + `npx tsc --noEmit` pass with no regression (D-15). [PERF-01]
- The SUS-flagged `lighthouse` package received a blocking-human legitimacy sign-off before any invocation (package-legitimacy protocol). [PERF-01]
- Lighthouse against a PRODUCTION build scores performance >=90 / accessibility 100 on home, `/shop`, and PDP, with scores documented in `10-12-SUMMARY.md` (D-15). [PERF-01]
</success_criteria>

<output>
Create `.planning/phases/10-trust-polish/10-12-SUMMARY.md` when done (include the per-route Lighthouse performance + accessibility scores table).
</output>